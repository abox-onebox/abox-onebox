# ABox 一盒 · 运维 crontab 片段（M5-12）
#
# ⚠️⚠️ 本文件是**模板**，不是可以直接 `crontab <file>` 喂进去的东西：
#      占位符 {{ABOX_REPO}} / {{ABOX_LOG_DIR}} 必须由 `scripts/install-cron.sh` 替换。
#
# ## 唯一真相纪律
# 全部「服务器周期性运维动作」**只在本文件声明一次**，由安装器渲染后写入用户 crontab。
# 请**不要** `crontab -e` 手工加条目 —— 那样就出现第二份表述，
# 而 crontab 里那份**不在任何对账路径上**（改了没人知道、删了也没人知道）。
# 同一形态在本项目已出现过多次：`TASK_SCHEDULES` 的 cron 与 `time.ts` 的锚点
# 各写一遍（缺陷 #49）、迁移与实体各写一遍（#76）、状态机声明与写入点各写一遍（#79）。
#
# ## 用法
#   安装：  ./scripts/install-cron.sh             # 幂等：先删旧块再写新块
#   预览：  ./scripts/install-cron.sh --dry-run   # 只打印渲染结果，不写 crontab
#   查看：  ./scripts/install-cron.sh --list
#   移除：  ./scripts/install-cron.sh --uninstall
#
# ## 时区（重要）
# cron 用的是**宿主机本地时区**，本产品业务时区统一 Asia/Shanghai。
# 这里**刻意不设 `CRON_TZ`**：设了就成了第三个时区来源（容器 TZ / 宿主机 TZ / CRON_TZ），
# 而「跑批按时区错一小时」在本项目里是**不报错**的（见《部署运维手册》§6 禁发窗口）。
# 正确做法：把宿主机（或 compose 的 `TZ`）固定为 `Asia/Shanghai`，让三者天然一致。
#
# ## 两个动作的时刻为什么定在这里
#   · 备份 **05:00** —— 8 个跑批任务最晚的是 04:00 对账，05:00 落备份；
#     4.x 的结算链（02:00 佣金 / 02:10 应付）此时都已收口，不会备份到「跑了一半」的账。
#   · 巡检 **08:30** —— 人上班之前出结论；且此时备份与跑批都已落定，
#     「昨晚到底跑没跑」这件事已经有结论，不是让巡检去猜一个还在进行中的状态。
#
# >>> ABOX OPS (managed by scripts/install-cron.sh · 手工修改会在下次安装时被覆盖) >>>

# ── 数据库备份（每日 05:00，跑批全部结束之后）──────────────────────────────
0 5 * * * {{ABOX_REPO}}/scripts/backup-db.sh >> {{ABOX_LOG_DIR}}/backup.log 2>&1

# ── 日巡检（每日 08:30，人上班前出结论）────────────────────────────────────
# ⚠️ 退出码语义：0 = 通过（含仅有 WARN）· 1 = 有 FAIL · 2 = 用法/环境错误。
#    故它可直接被监控系统按退出码消费，不需要解析日志文本。
30 8 * * * {{ABOX_REPO}}/scripts/ops-daily.sh >> {{ABOX_LOG_DIR}}/ops-daily.log 2>&1

# <<< ABOX OPS <<<

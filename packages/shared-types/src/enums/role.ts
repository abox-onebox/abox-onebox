/** 4 角色（L2：无楼长） */
export enum Role {
  USER = 'user',
  LEADER = 'leader',
  SUPPLIER = 'supplier',
  ADMIN = 'admin',
}

/** 3 载体（C7） */
export enum Carrier {
  MINIPROGRAM = 'miniprogram',
  SUPPLIER_WEB = 'supplier_web',
  ADMIN_WEB = 'admin_web',
}

# ABox 一盒 · 开发里程碑计划 v1.0

> 版本：v1.1.12（就地修订 · 文件名保持 v1.0）· 2026-09-17
> 适用对象：开发专家团 / 人类开发团队
> 上游依据：《ABox一盒开发前准备计划v1.0.html》（W1–W2 准备期）+《开发基线冻结清单v1.0.md》+《协作规范 v1.0》
> 范围边界（L / §七）：本期只做**一个办公楼的完整闭环** —— 下单 → 截单 → 出餐 → 配送 → 取餐 → 结算 → 看板

---

## 〇、总览：W1–W10

```
周次      W1        W2        W3   W4     W5      W6     W7      W8     W9     W10
        ├─准备期──┤
阶段一  ████
阶段三      ████（长周期资源，并行）
阶段二        ████
阶段四            ████
启动评审              ★
                        ├────── 开发期 ──────┤
M1 地基+用户端            ████████
M2 团长端                          ████
M3 供应商端+后台                          ████████
M4 结算+任务+消息                                  ████
M5 联调+灰度上线                                        ████
试运营观察                                                    ████
```

| 里程碑 | 时间 | 一句话目标 | 出口标志 |
| --- | --- | --- | --- |
| **M0** | W2 末 | 冻结基线、正式引入开发 | 《启动评审纪要》+ `v1.0-baseline` tag |
| **M1** | W3–W4 | 地基打通 + 用户端主链路可走通 | 能下单、能支付、能看订单、截单前能取消 |
| **M2** | W5 | 团长端全链路可用 | 能接单、能分发、能确认、佣金可见 |
| **M3** | W6–W7 | 供应商端 + 运营后台可用 | 出餐可确认、套餐可编排、订单可管控 |
| **M4** | W8 | 结算 / 定时任务 / 消息贯通 | 次日跑批自动入账、消息按策略触达 |
| **M5** | W9 | 三方联调 + 单楼灰度上线 | 一个办公楼完整闭环跑通 3 天无 P0 |

---

## 一、M1 · 地基打通 + 用户端主链路（W3–W4）

### 目标
打通"登录 → 下单 → 支付 → 看订单"四条骨架，让用户端**主链路在真机上无断点**。

### 任务分解

| # | 任务 | 落点 | 依赖 |
| --- | --- | --- | --- |
| 1.1 | Monorepo 依赖安装 + 本地环境跑通（`setup.ps1`） | 仓库根 | — |
| 1.2 | 数据库迁移：**24 张表**建齐（含 4 张补齐 DDL） | `api-server/migrations` | ER v2.1 + 评审意见 |
| 1.3 | 种子数据导入（12 楼 / 5 楼群 / 4 供应商 / 4 集散中心 / 菜品库 / ¥25.80 / 4 级佣金） | `database/seeds` | 1.2 |
| 1.4 | 微信登录（`code2session`）+ JWT 下发 + `auth.guard` | `modules/auth` | 1.2 |
| 1.5 | 统一响应 / 异常 / 校验管道 + Swagger 骨架 | `common/*`、`main.ts` | — |
| 1.6 | 套餐首页接口 U1–U3（含分配矩阵命中的套餐） | `modules/meal` | 1.3 |
| 1.7 | 下单接口 U7 + 幂等键 + 30 分钟未支付超时取消 | `modules/order` | 1.6 |
| 1.8 | 微信支付 JSAPI 下单 + 回调验签 | `modules/payment` | 1.7 |
| 1.9 | 订单列表 / 详情查询（三视角文案映射） | `modules/order` | 1.7 |
| 1.10 | 截单前自助取消（走 T4，截单后拒绝 `40004`） | `modules/order` | 1.7 |
| 1.11 | 小程序 P1 / P3 / P4 / P5 / P6 / P7 页面实现 | `miniprogram/src/pages` | 1.6–1.10 |
| 1.12 | 截单倒计时（`use-countdown`，真实到 T-1 24:00） | `composables` | — |

### 交付物
- 可运行后端（`pnpm dev:api`）+ Swagger 已含 U1–U11
- 微信开发者工具可打开的小程序，用户端主链路可点击走通
- 单测：状态机、下单幂等、取消窗口

### 验收标准
1. 微信授权后直接进入首页，**不索要手机号 / 地址**（L9）
2. 下单 → 支付成功 → 支付结果页 → 订单详情，状态依次为 `pending_pay → paid`，与《状态机 v1.0》§1.2 三视角文案一致
3. 截单前取消成功、金额原路退回；**截单后调取消接口返回 40004**
4. 同一幂等键重复下单只产生一单
5. 倒计时显示距当日 24:00 的真实剩余时间

### 风险
微信支付商户号实际可用性（**已于准备期核对，若额度/权限异常将阻塞 1.8**）

---

## 二、M2 · 团长端全链路（W5）

### 目标
跑通"团长看到本楼单量 → 接收取餐提醒 → 一键分发 → 佣金入账 → 提现"，并落地**代退申请**入口。

### 任务分解

| # | 任务 | 落点 | 依赖 |
| --- | --- | --- | --- |
| 2.1 | 团长叠加身份（`isLeader` 服务端判定 + 本地持久化） | `modules/team-leader` | M1 |
| 2.2 | 团长申请：提交即生效（C3） | `modules/team-leader` | 2.1 |
| 2.3 | 工作台 / 本楼概况 / 订单明细（P11–P13） | `miniprogram/pages/leader` | 2.1 |
| 2.4 | **代退申请**（C6 第一段：只登记，不退款不回退） | `modules/finance/refund` | 2.3 |
| 2.5 | 取餐确认 + 一键分发（按**实发份数**计入佣金） | `modules/order` | 2.3 |
| 2.6 | 佣金中心 / 余额流水 / 提现申请（P16–P18） | `modules/finance` | 2.5 |
| 2.7 | 分享中心 + 小程序码生成（P19） | `modules/team-leader/share` | 2.1 |
| 2.8 | 团长资料 + 退出团长身份（P20） | `miniprogram/pages/leader` | 2.1 |
| 2.9 | 推荐裂变与晋级审计（"介绍 N 名转正"数据落表） | `modules/team-leader/invite` | 2.2 |

### 交付物
- 团长端 P11–P20 全部可点击
- `ab_leader_invite` 表落库，晋级审计可查
- 单测：代退申请**不改订单金额、不触发退款**；佣金按实发份数计算

### 验收标准
1. 用户在"我的"可申请团长，**勾选协议后立即生效**，tabBar 出现团长入口（无审核流程）
2. 团长对本楼订单发起代退申请后：`ab_refund.status='applying'`，订单进入 `refund_applying`，**资金未动**
3. 一键分发后佣金按 `实发份数 × 等级费率` 计算，与余额流水一致
4. 提现申请可提交并进入待审批

### 风险
佣金基数口径（实发 vs 下单）—— 已定稿"按实发"，实现若用错会导致全站佣金偏差

---

## 三、M3 · 供应商端 + 运营后台（W6–W7）

### 目标
让运营能**编排套餐 → 管订单 → 管团长/供应商/办公楼**，让供应商能**看出餐量 → 确认出餐 → 查应付**。

### 任务分解

| # | 任务 | 落点 | 依赖 |
| --- | --- | --- | --- |
| 3.1 | 后台登录 + 角色菜单过滤（admin / supplier 同工程） | `admin-web/router` | — |
| 3.2 | 套餐矩阵（日期 × 楼群）+ 新建/编辑套餐 | `views/meal` | M1 |
| 3.3 | 订单中心（全部 / 异常订单 Tab）+ 订单详情 | `views/order` | M1 |
| 3.4 | **后台审批退款**（C6 第三段：通过→退款；驳回→回原状态） | `views/order` + `modules/finance` | M2 |
| 3.5 | 团长名录 + 申请流水（无审核，仅流水） | `views/leader` | M2 |
| 3.6 | 供应商管理 + 菜品库 + **集散中心配置（C4 表驱动，默认 4 可增删）** | `views/supplier` | M1 |
| 3.7 | 办公楼管理 5 视图（含团长绑定、配送映射 R1–R4） | `views/building` | 1.3 |
| 3.8 | 出餐确认（供应商端，**须 ≤ 09:30**） | `views/order`（supplier） | M1 |
| 3.9 | 供应商应付结算明细（P25）+ 财务总览 | `views/finance` | 3.6 |
| 3.10 | 数据看板（核心指标 / 榜单 / 菜品热度 / 留存） | `views/stats` | 3.3 |
| 3.11 | 系统配置（¥25.80 / 4 级佣金 / 截单 T-1 24:00）+ 操作日志 | `views/system` | — |
| 3.12 | **通知模板**（5 场景 · 渠道 / 启用闸门 / 变量白名单 · `ab_message_template`） | `views/system` + `modules/message` | 3.11 |
| 3.13 | **财务端点 D33–D35**（资金总览复用看板口径 / 佣金明细（快照）/ 佣金入账补跑） | `views/finance` + `modules/finance` | 3.9 |
| 3.14 | **余额账户管理与调整 D38–D39**（全量负债复用 D33 单一函数 / 四动作（充 · 扣 · 冻 · **解**）/ 乐观锁 + 幂等键必填 / 新枚举 `ab_balance_log.type='adjust'`） | `views/finance` + `modules/finance` | 3.13 |
| 3.15 | **对账 D43 + 发票 D44**（差异清单 + 五类可执行差异 / `date` = **支付日** / 一期诚实标注「**未与微信侧对平**」/ 发票从应付单**派生**（零 DDL）· 粒度 = 供应商 × 月份 · 三态（`partial` 是重点）· 分母只含已付款 · 逾期与抬头缺失提示） | `views/finance` + `modules/finance` | 3.14 |

### 交付物
- 后台 25+ 视图可操作，权限过滤生效
- 供应商端 P21–P26 可用
- 单测：退款审批三段式、越权访问 403、集散中心增删驱动结算费用项

### 验收标准
1. `role=supplier` 登录后**看不到运营菜单**，且无法通过 URL 越权访问
2. 后台审批通过退款 → 微信退款发起 → 订单转 `refunding → refunded` → **反向结算**（供价/场地费/打包人工/配送费冲减应付 + 佣金写负向流水 + 毛利留存）
3. 集散中心配置新增一个，结算计算随之生效（**不得硬编码 4 个**）
4. 01:00 前未出餐的订单在后台标 `warning`，但**订单状态不回退**

---

## 四、M4 · 结算 / 定时任务 / 消息（W8）

> ✅ **M4-0 前置（契约级修正）已完成 · 2026-09-16** —— 见版本表 v1.1.5：
> ① `ab_distribution_center.supplier_id` 改**可空历史字段**；② `ab_supplier.type` **转历史字段** + **D27 路由删除**（`50008` 三处闸门拆除、号位保留）；
> ③ ⭐ **S3 打包任务整条迁运营后台** `GET /admin/packing-tasks`（P39「加工场所打包」）—— 原判据在 `supplier_id` 停用后失效，且其数据面**跨供应商**（开给任一供应商即泄露同业经营数据，违反 I1），**故迁移而非换判据**（《缺陷与陷阱》#58）。
> 零新增列 / 零新表（表数仍 **27**），门禁 `all` 11/11 + `verify` 4/4 全绿。
>
> ✅ **M4-1 日切链路已完成 · 2026-09-17** —— 见版本表 v1.1.6：**4.1 / 4.2 / 4.3 三个日切任务实装** +
> **调度基座 `TASK_SCHEDULES`**（cron 与「目标日期语义」分开声明，杜绝「同刻不同日」）+ **手动补跑接口**
> （`GET /admin/schedule`、`POST /admin/schedule/{task}/run`），并把已实装的 4.6 / 4.8 迁到统一执行口。
> ⭐ 顺带修掉一个真实数据缺口：`ab_meal_assignment.sold_count`（备料量基数）**全仓无累加点**，
> 跑批推给供应商的份数**恒为 0 且不报错** → 改为**截单时定格**并**覆盖刷新**生产计划
> （《缺陷与陷阱》#59 / #60 / #61）。**零 DDL、零新增错误码**；e2e 新增 **§28 · 30 条断言**（不依赖下单窗口）。
>
> ✅ **M4-2 结算链路已完成 · 2026-09-17** —— 见版本表 v1.1.7：**4.4 / 4.5 / 4.7 三个任务实装**，
> 8 个定时任务**全部补齐**（`GET /admin/schedule` 的 `summary.implemented` = **8/8**）。
> ⭐ 头号口径变更是**佣金两段式**：**T 日 14:00 计佣**（写 `pending`、不动余额）→ **T+1 02:00 入账**（`settled` + 进余额）
> —— 计佣与入账是**两个动词、两处落点**；由此**修掉一个静默错账**：旧实现「确认即入账」在退款落在
> 两段之间的窗口时会**从余额扣一笔从未入账的钱**（余额允许为负 → 形成假欠款且不报错）
> → 冲销拆成**两条路径**（`pending` 只作废原行 / `settled` 才写负行 + 扣余额）（《缺陷与陷阱》#62）。
> ⭐ 同批修掉 `monthOrdersOf` 的**双计**（退款单被 `status` 排除后又被 `CASE` 加回 → 月单不降反平、
> 可凭退款刷 C2 晋级，#63），并把 `reconciliation` 的目标日期由 `today` 改正为 `yesterday`
> （04:00 对「今日」只核 4 小时切片 → 每天都漏核一段，#64）。**零 DDL、零新增错误码**；
> e2e 新增 **§29 · 10 条不变量**，§28 三条「未实装」断言翻转为 8/8。
> ✅ **M4-3 队列消费者 + 订阅消息投递点已完成 · 2026-09-17** —— 见版本表 v1.1.8：**4.9 三消费者实装**，
> 并把**外部支付通道调用移出 DB 事务**（退款改为「事务内只落账务 → 事务外调通道 → 失败入队退避重试」，
> `refundNo` 作微信幂等键（`out_refund_no`），**重试不重复出款**）；**4.10 投递点贯通**：新增 `leader_apply`
> （团长申请确认）与 `commission_settled`（佣金入账）两个**真实投递点** + 用户端订阅授权清单接口
> （`GET /me/subscribe/templates`）。
> ⭐ **范围刻意收窄**：里程碑原文列的四项场景里，`user_order_status`（简化原则不推）/ `leader_delivery`
> （走微信群人工）/ `merchant_cook`（收件人是**供应商**，不是用户小程序身份）三者**接了也没有真实收件人**
> → 只做 `leader_apply` + `commission_settled`，其余**如实标 `pending`**（**宁失真绿，不假装贯通**）。
> **零 DDL、零新增错误码**；e2e 新增 **§30 · 68 条断言**（含一条跨端落地页一致性机械对账）。
> **至此 M4 全部收口（4.1–4.12）。**
> ✅ **M4-4 提现审批（D45 / D46–D46c · P34 · 模块 M35-08）已完成 · 2026-09-17** —— 见版本表 v1.1.9：
> 补齐 **L12（提现申请）打开的资金链在后台的收口**：**D45** 提现审批队列（三分 Tab：待审批 / 待打款 / 已终态）
> + **D46** 批准 / **D46a** 驳回 / **D46b** 到账回执 / **D46c** 打款失败。
> ⭐ 本批之前后台**没有任何端点能推进提现单** → 被冻结的钱既出不去也回不来，
> 且 C3 退团守卫会以「有未完成的提现」把团长**永久困住**（错误文案还写着「等待提现到账」）。
> ⭐ 三条资金不变量：**三路解冻共用一个 `releaseFrozen()`**（驳回/失败 = `balance +X / frozen −X`
> 且两累计都不动；到账 = `frozen −X / total_out +X` 且**按申请金额**而非实付累加）；
> **到账不写 `ab_balance_log`**（该表语义是「可用余额的每一次变化」，到账时可用余额不变）。
> ⭐ 同批修掉一条**用户直接看得见的错**（《缺陷与陷阱》#69）：`ab_team_leader.balance` /
> `withdrawn_amount` / `pending_amount` 三列**从种子之后全仓没有任何写点**，却被登录 / 资料 / 退出
> 三处出参当作「可用余额 / 已提现 / 待入账」下发 → 团长「我的」页显示的是种子死数字，与「余额明细」页
> 显示**两个不同的余额**，去提现又被 `50004` 拦下；真源收敛到 `LeaderMoneyService`
> （`ab_balance` / `ab_commission` / `ab_withdraw` 派生），三列转**历史字段**（列保留 · 不再读写）。
> **零 DDL**；新增错误码 `40016` / `40017`；e2e 新增 **§31 · 49 条断言**（不依赖下单窗口）。

### 目标
把"夜里自动跑批、次日自动入账、关键节点自动触达"打通 —— 这是**不依赖人操作**的兜底能力。

### 任务分解

| # | 任务 | 落点 | 依赖 | 批次 / 状态 |
| --- | --- | --- | --- | --- |
| 4.1 | `meal-publish.task`（T-1 14:00 开团） | `tasks` | M3 | **M4-1 ✅ 已实装** |
| 4.2 | `cutoff.task`（T-1 24:00 截单：取消未支付 + 锁定 + 推备料量） | `tasks` | 4.1 | **M4-1 ✅ 已实装** |
| 4.3 | `delivery-generate.task`（T 日 00:30 生成配送单） | `tasks` | 4.2 | **M4-1 ✅ 已实装** |
| 4.4 | `auto-confirm.task`（T 日 14:00 自动确认 + 计佣 · **只转 `delivered`**） | `tasks` | M2 | **M4-2 ✅ 已实装** |
| 4.5 | `commission-settle.task`（T+1 02:00 佣金**入账**：`pending → settled` + 进余额） | `tasks` | 4.4 | **M4-2 ✅ 已实装** |
| 4.6 | `supplier-share.task`（T+1 02:00 生成应付，**不拨款**） | `tasks` | M3 | ✅ 已实装（M3-9 · M4-1 迁统一执行口） |
| 4.7 | `reconciliation.task`（每日 04:00 对账 · **核昨日**、按支付日切日） | `tasks` | M1 | **M4-2 ✅ 已实装** |
| 4.8 | **`leader-expire.task`（每日 03:00 见习团长 30 天未促单失效，C2）** | `tasks` | M2 | ✅ 已实装（M2 · M4-1 迁统一执行口） |
| 4.9 | Redis + BullMQ 三消费者（支付后续 / 退款申请 / 结算后续） | `queues` | 4.2 | **M4-3 ✅ 已实装**（`QUEUE_DRIVER` 双驱动 · memory 驱动**如实标 `durable=false`**、redis 连不上**拒绝启动**） |
| 4.10 | 微信订阅消息（支付成功 / 出餐提醒 / 取餐通知 / **退款结果必推**） | `modules/message` | 4.4 | **M4-3 ✅ 已贯通** —— 真实投递点 **3 个**（`refund_result` / `leader_apply` / `commission_settled`）；其余 3 个**如实标 `pending`**（理由见上：没有真实收件人） |
| 4.11 | 幂等与重试：所有任务以 `meal_date` 为幂等键，失败可重放 | `tasks` | 4.1–4.8 | **✅ 已全量验证** —— M4-1（日切 3 个）+ M4-2（结算 3 个）重跑均为「0 新增 / 0 重复加钱 / 告警不重复写」；**M4-3 补齐队列侧**：`refund-apply` 以 `refundNo`（微信 `out_refund_no`）为幂等键，**重试不重复出款**，重试耗尽进死信并写 `module=queue` 操作日志（**可重放**） |
| 4.12 | 财务"付款登记"（人工对公：登记回单号 + 发票号，改应付状态） | `views/finance` | 4.6 | ✅ 已实装（M3-9 · D37） |
| — | **D45/D46 提现审批**（后台 P34 · 模块 M35-08 · 收口 L12 资金链） | `views/finance` | 4.5 | **M4-4 ✅ 已实装** |
| — | **调度基座 + 手动补跑接口**（`GET /admin/schedule` · `POST /admin/schedule/{task}/run`） | `tasks` | 4.1–4.8 | **M4-1 ✅ 已实装** |

### 交付物
- 8 个定时任务上线，可控触发（含手动补跑接口）
  → ✅ **M4-1（4.1/4.2/4.3）+ M4-2（4.4/4.5/4.7）已全部实装**，`GET /admin/schedule` 报 **8/8**
- 消息模板在后台可配置 → ✅ 已实装（M3-12 · D59–D60）；**投递点已于 M4-3 贯通 3 个真实场景**（退款结果 / 团长申请 / 佣金入账）
- 单测：任务幂等（重复触发只生效一次）、反向结算金额、佣金入账金额
  → ✅ **M4-1 已验**（三任务重跑 `skipped` / 0 新增）；✅ **M4-2 已验**（e2e §29：入账金额取 `ab_commission` **快照**、反向结算「金额正 / 份数负」，且 `pending` 与 `settled` 两条冲销路径各自钉死）

### 验收标准
1. 模拟 T-1 24:00 触发截单：未支付订单全部 `cancelled`，已支付转 `cut_off`，4 家供应商收到备料量
   → ✅ **M4-1 已验**（e2e §28：三分支 + 解冻余额 + `sold_count` 定格 5 份 + 计划量按定格量覆盖 + 幂等重跑）
2. T 日 14:00 未确认的订单自动 `completed` 且佣金入账（**不依赖团长操作**）
   → ✅ **M4-2 已验**（e2e §29：**只转 `delivered`**、其余态 fail-closed 原样不动；计佣写 `pending`、T+1 02:00 入账 `settled` + 进余额；无归属团长收口计佣 0）
3. 每个任务重复触发 **不产生重复数据**
   → ✅ **M4-1 已验**（开团 / 截单 / 配送单三个任务各自重跑，`published=0` / 取消 0 锁定 0 / `skipped=1`）
   → ✅ **M4-2 已验**（自动确认 / 佣金入账 / 对账三个任务各自重跑：`confirmedCount=0`、`settled=0` 且余额与流水条数均不变、对账不平告警**不重复写**）
4. 退款结果**必推**订阅消息（用户可感知）→ ✅ 已验（M3-12 §23）；✅ **M4-3 已补**：投递点含团长申请 / 佣金入账
   （e2e §30），用户端可拉到「当前可授权清单」，且服务端下发的落地页与 `pages.json` 做**机械对账**
5. 供应商应付单生成后**不自动打款**，财务在后台登记回单号后状态转 `success` → ✅ 已验（M3-9）
6. **外部通道（微信退款）不得与 DB 事务同生共死**：通道失败时账务已定稿、任务已入队重试、重试耗尽进死信
   → ✅ **M4-3 已验**（e2e §30 注入通道失败：返回 `code=0` + `wxDelivered=false` / 单据 `failed` / 订单仍 `refunded`；
   3 次后进死信 + `module=queue` 操作日志，快照带**可重放 hint**）
7. 队列状态**如实报告「会不会丢任务」** → ✅ **M4-3 已验**（`GET /admin/queue` 在 memory 驱动下报 `durable=false`，
   **不做静默降级**；`failed` 口径 = **重试耗尽进死信**，不是「失败过一次」）

---

## 五、M5 · 三方联调 + 灰度上线（W9）

### 目标
一个办公楼（建议**国贸三期 A 座**，示例团长李明所在楼）真实闭环跑通。

### 任务分解

> **进度（2026-09-17）**：M5 的 5.1–5.7 里有 4 项**强依赖云资源与真实账号**（5.2 压测需生产级环境、5.4 部署需服务器与域名、5.5 灰度需真实楼与真实团长、5.6 演练需真实通道），
> 这些外部条件**用户侧尚未到位**（见《账号资源与密钥清单 v1.0》）。故本地先推进**不依赖外部条件的那部分**，已落三个子批次：
> **M5-0「部署运维基座」**（把部署所需的代码与脚本产物全部做完并静态验收，使外部条件到位后**只需执行、无需再开发**）、
> **M5-1「配送单人工修正」**（把只能进库改的运力/份数口子收回运营手里）、
> **M5-2「生产迁移补齐 + 结构对账门禁」**（补掉「迁移比实体少 2 表 9 列」的缺口，并加机械对账防复发）。
> ⚠️ **子批次 ≠ M5** —— 里程碑验收标准（3 天闭环 / 账实相符 / 回滚 10 分钟内）**必须真机执行**，未执行前 M5 **不置完成**。

| # | 任务 | 说明 | 本地可推进性 |
| --- | --- | --- | --- |
| 5.1 | 端到端联调（用户端 / 团长端 / 供应商端 / 后台四条链路交叉验证） | 按《状态机 v1.0》§七 状态↔接口↔表 对照逐项过 | ✅ 已由 e2e m1/m2/m3 覆盖（**45 / 126 / 951** 断言） |
| 5.2 | 压测 / 稳定性（当日单量峰值、跑批窗口不重叠） | 目标：单楼 500 单/日无超时 | ⏳ **阻塞：需生产级环境**（本地 sqlite 单进程无参考价值） |
| 5.3 | 安全自检（越权、幂等、重放、密钥泄露、日志脱敏） | 对照《协作规范 v1.0》§五 | 🔶 **部分已做**（越权 / 幂等 / 重放已被 e2e 钉死；免鉴权探针出参脱敏已验）—— **HTTPS 与白名单项待环境** |
| 5.4 | 生产环境部署（Docker + K8s）+ 域名 / HTTPS / 白名单核对 | 见《账号资源与密钥清单 v1.0》 | 🔶 **M5-0 已交付全部产物**（镜像 ×2 / compose / nginx / 5 脚本 / 运维手册）；⏳ **真机执行待服务器与域名** |
| 5.5 | 灰度：单楼开团 → 观察 3 天 | 每日核对：订单数、结算等式闭合、佣金、退款链路 | ⏳ **阻塞：需真实楼栋与真实团长账号** |
| 5.6 | 应急预案演练（回滚、截单窗口故障、支付回调异常） | 禁发窗口：T-1 23:30–24:30、T 日 10:00–12:30 | 🔶 **M5-0 已写预案 6 类 + 回滚 / 备份 / 恢复脚本**；⏳ **演练待环境** |
| 5.7 | 试运营交付与培训（团长 1 页操作卡 + 供应商 1 页出餐卡） | — | ⏳ 待 5.5 灰度后产出 |

### 子批次 M5-0 · 部署运维基座（2026-09-17 · 本地已完成 · 静态验收）

| 交付物 | 内容 |
| --- | --- |
| 探针分级 | `GET /health`（存活 · **不检依赖**）出参**保持不变** + 新增 `GET /health/ready`（就绪 · 检 DB 与 3 个队列消费者） |
| 优雅关闭 | `main.ts` 补 `enableShutdownHooks()`（此前两处 `onModuleDestroy` **从未执行过**）+ 容器 **exec 形式 CMD**（shell 形式会吞 SIGTERM） |
| 生产迁移 | 迁移 glob 修正为 `*.{ts,js}` **双匹配** + `db:migrate:dist` 脚本（原 `.ts` 单匹配在生产 `dist/` 下**静默零命中、退出码仍为 0**）；⚠️ 修好之后首迁**会真跑** → 顺带暴露出「迁移比实体少 2 表 9 列」，已在 **M5-2** 补齐并加对账门禁 |
| 镜像 | `apps/api-server/Dockerfile` 多阶段重写（非 root / tzdata / HEALTHCHECK / 锁文件冻结 / `ARG APP_VERSION`）+ `apps/admin-web/Dockerfile` + `nginx.conf` |
| 编排 | `docker-compose.prod.yml`（四服务 · 依赖不暴露宿主端口 · `:?` fail-fast 密钥校验）+ `.dockerignore` + `.gitignore` 补密钥与备份排除 |
| 运维脚本 | `deploy.sh`（**原为空壳**，重写 7 步）· `rollback.sh` · `backup-db.sh` · `restore-db.sh` · `healthcheck.sh` |
| 手册 | 《ABox一盒部署运维手册 v1.0》（环境矩阵 / 拓扑 / 密钥纪律 / 部署流程 / 禁发窗口 / 监控 / 备份恢复 / 应急预案 6 类 / 日巡检 / 遗留风险 9 条） |
| 回归保护 | 探针契约断言并入 `e2e-m1.mjs`（**45/45**，此前 38）—— 防止「改了探针出参、三个套件静默全红」 |

> ⚠️ **遗留风险（未真机执行，如实标注）**：本机**无 Docker**，故上述镜像与脚本**只做过静态审查（52 项机械检查 + `bash -n`）**，**未在真实服务器上跑过**。
> 另：生产库**从未执行过迁移**、e2e 用的是 sqlite（非 MySQL）、微信回调与 HTTPS 均未在真实通道验过、未做压测、镜像未瘦身。九条风险逐条列在《部署运维手册 v1.0》§遗留风险，**不得当作已解决**。

### 子批次 M5-1 · 配送单人工修正（2026-09-17 · 本地已完成 · e2e 实证 · 零 DDL · 新增 30016/30017 · 表数仍 27）

| 交付物 | 内容 |
| --- | --- |
| D61 列表 | `GET /admin/deliveries?date=&buildingGroupId=&status=` —— `date` 缺省取「最近一个有配送单的日子」（不写死今日）；每行带 `orderQuantity`（截单实收份数）/ `quantityDiff` / `quantityMismatch` / `hasManualInput` / `statusText` / `version` |
| D62 修正 | `PUT /admin/deliveries/{id}` —— 只改 5 个字段（`totalQuantity` / `driverName` / `driverPhone` / `plateNo` / `remark`），**不含 `status`**（状态属履约流转，另批） |
| 乐观锁 | `version` 必填，`WHERE id=? AND version=?` CAS；冲突 → **`30016` + `data.current`**；目标不存在 → **`30017`** |
| 审计 | 出参带 `before / after / changed`，经 `OperationLogInterceptor` 的 `response_data` **整体落库** → **不新建审计表**（快照列本就只存 `{operator, role, at}`，diff 靠响应体承载） |
| 份数对比 | 与 4.3 跑批**共用** `aggregateOrderQuantity()`，且**取全量**（不受列表筛选影响）；`note` 明写「**份数不符 ≠ 被人改过**」（截单没跑成 / 人工修正 两种成因**不区分**） |
| 枚举收敛 | 删掉 `leader-order.service.ts` 与 `workbench.service.ts` 里逐字重复的 `DELIVERY_STATUS_TEXT`，收敛到 `shared-types/src/enums/delivery-status.ts`（否则新建的后台页会成为**第三份**） |
| 权限 | 类级含 `operator`（要跟单），**不含 `viewer`**（含运力与司机电话）、不含 `finance`；无 POST / DELETE（配送单由跑批生成，不做手工增删） |
| 前端 | `views/order/delivery.vue`（列表 + 修正弹窗：提交前显示 `before → after` diff、`reason` 必填、`30016` 自动刷新重提）+ 路由 `/order/delivery` + `ADMIN_NAV` + `ADMIN_MENU_KEYS` |
| 回归保护 | e2e **§32**（25 条断言 · 隔离日 −216/−217 · 节末全量还原）；§28 的「人工录入」由**直写 SQL** 改为**走 D62 接口** —— 顺带让「修正真的落到库里」成为一条断言 |

> ⚠️ **未做的事（如实标注）**：① **修 `status`**（发货 / 在途 / 到达）**没做** —— 属履约流转，与本次「修数据」两种风险不同，留独立批次；② `quantityMismatch` 是**有歧义**的标记（见上），真正可靠的人工痕迹标记是 `hasManualInput`；③ **#49（时间类配置与调度分家）本批只做文案纠偏**（`unwiredReason` 由「硬编码在 `@Cron`」改为指向 `TASK_SCHEDULES` 声明表），**接线调度仍为独立验收项** —— 它会动下单窗口，而 e2e 全量依赖 14:00–23:00。

### 子批次 M5-2 · 生产迁移补齐 + 结构对账门禁（2026-09-17 · 本地已完成 · 收口 #76 · 新增 1 支迁移 · 零错误码 · 表数仍 27）

> **一句话**：全仓唯一那支迁移比实体**少 2 张表 / 9 列 / 1 索引**，且**从未在任何环境执行过** —— 本批把缺口补上，并加一道**机械对账门禁**让它**不可能再漂**。

| 交付物 | 内容 |
| --- | --- |
| 增量迁移 | `1700000000001-parity-fix.ts` —— 补 **2 表**（`ab_supplier_dish_center_daily` §3.6.1 · `ab_message_template` §3.9）+ **9 列**（`ab_building.population` · `ab_supplier` 审核族 7 列 · `ab_refund.order_status_before`）+ **1 索引**（`ab_building.idx_building_status`）+ `ab_building.status` **注释对齐**（三态） |
| 幂等守卫 | MySQL 8 **不支持** `ADD COLUMN IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS` → 列与索引逐项查 `information_schema.COLUMNS` / `.STATISTICS`（`TABLE_SCHEMA = DATABASE()`，不串库）后再加；两张表用 `CREATE TABLE IF NOT EXISTS`。**重跑不报错、不重复加** |
| 为什么是增量 | init **从未被执行过**，就地改它更干净 —— 但**若某环境已把 `Init1700000000000` 记为已执行**，TypeORM 会**静默跳过**改动后的它，缺口依旧。增量迁移**不再依赖「没人跑过」这个前提** |
| 对账门禁 | `apps/api-server/src/database/schema-parity.ts` + `gate.mjs` 新增 **`schema:parity`** —— 源A「**迁移推演**」（解析全部迁移的 `CREATE TABLE` / `ADD COLUMN` / `MODIFY COLUMN`）≡ 源B「**实体真库**」（TypeORM 拿 `ALL_ENTITIES` 在内存 sqlite `synchronize` 真建表读回） |
| 门禁设计要点 | ① 源B **刻意不解析实体源码** —— 列装饰器形态多（`@Column`/`@VersionColumn`/`@PkColumn`/`@CreateDateColumn`），正则解析必漏（**本批前两版解析器就是这么错的**）；② **PK 列豁免类型比较** —— sqlite `integer` vs mysql `bigint` 是 `PkColumn()` 按 `DB_DRIVER` 的**刻意分派**；③ 未登记类型 / 未支持语句形态（`CHANGE COLUMN` / `RENAME` / `CREATE INDEX` / `DROP COLUMN`）**显式报错、不静默跳过**；④ 关键词扫描**一律建立在剥注释后、且只取 `up()` 作用域**的文本上（否则迁移里的一句注释就能触发假报） |
| 自证能力 | 检查器**每次运行人为制造一个缺口**（从源A 深拷贝里删掉一列）**并确认能报出该列**，否则自身失败 —— **「恒绿的检查比没有检查更糟」**（本批自检就抓出了自身一个跨文件计数 bug） |
| 门禁纳入 | `gate.mjs` 的 `GATES` 增 `schema:parity` · `all` 别名数组同步 → **15/15 变 16/16** |
| 文档回写 | ER 文档 §5.3 `ab_refund.order_status_before` **`tinyint` → `VARCHAR(16)`**（原文档记录有误）+ 新增 §5.8 迁移↔实体对齐段；init 头注释「24 张表」纠正为**实际 25 张**并注明差额去向；运维手册 **R2 升级** |

> ⚠️ **未做的事（如实标注）**：① **仍未在真机空库上跑过首迁** —— 本机无 Docker / 无 MySQL，故本批验的是「**迁移推演 ≡ 实体**」这件**结构事实**，不是「MySQL 上执行成功」这件**运行事实**；运维手册 R2 由「未验证」升级为「**结构已机械对账** + 仍需真机空库首迁预演」，**不写成已验**；② **`down()` 只能回退本支**（`parity-fix`），`init` 的 `down()` 保持不变；③ **两支迁移合并推演才等于实体** —— 单看 init 会少 2 表 9 列，这是刻意的（见 init 头注释）。

### 验收标准

> ⚠️ 以下 4 条**全部要求真机执行**，M5-0 / M5-1 / M5-2 三个子批次的完成**均不构成**对它们的任何满足
> （尤其 M5-2：它保证的是「迁移推演 ≡ 实体结构」，**不等于**「MySQL 上首迁成功」）。
1. 一个办公楼完整跑通 **3 个自然日**（含一次完整跑批与一次退款）
2. 单份结算账实相符（等式闭合）：**协商供价（示例 ¥14.00）+ 场地费（默认 ¥0）+ 打包人工 + 配送费 + 佣金（按等级）+ 毛利（结果值）= ¥25.80**
3. 无 P0 缺陷；P1 缺陷 ≤ 3 且均有临时规避方案
4. 回滚演练成功（10 分钟内可回退上一个版本）

---

## 六、关键路径与缓冲

```
关键路径：1.2 建表 → 1.3 种子 → 1.6 套餐接口 → 1.7 下单 → 1.8 支付
        → M2 取餐确认（佣金基数）→ 3.4 退款审批 → 4.5/4.6 结算跑批 → M5 上线
```

| 缓冲位 | 预留 | 用途 |
| --- | --- | --- |
| W7 末 | 0.5 天 | 后台返工缓冲 |
| W8 末 | 0.5 天 | 跑批联调缓冲 |
| W9 | 1 天 | 上线前救火 |

**不可压缩项**：1.2（表结构）与 4.5/4.6（结算跑批）—— 这两处出错会导致账不平，返工代价最高。

---

## 七、风险登记册

| # | 风险 | 概率 | 影响 | 应对 | 责任 |
| --- | --- | --- | --- | --- | --- |
| R1 | 供应商发票链条未闭环 | **低** | 中 | 结算周期**已定案＝日结**（C11）；系统已按「人工对公 + 应付单」设计；仅剩发票按期开具需财务盯 | 财务 |
| R2 | 灵活用工平台对接细节（API / 费率 / 到账周期）未确认 | **低** | 中 | 个税方案**已定案＝灵活用工平台**（C11）；出款通道已抽象 `PayoutChannel`，一期「清单导出 + 回执登记」，**不阻塞 M1**；W1 完成开户与费率确认即可 | 项目负责人 + 财务 |
| R3 | 云资源未开通 | 低→已消解 | 高 | 准备期已启动开通，M1 前必须可用 | 运维 |
| R4 | **灵活用工平台**开户 / 费率 / 到账周期确认延迟 | **低** | 中 | 已定案（C11）；通道已抽象，一期可「余额记账 + 清单导出 + 人工提交」，不阻塞 M1；微信「商家转账到零钱」已不再需要 | 项目负责人 + 财务 |
| R5 | 楷体在小程序真机降级不美观 | 中 | 低 | `wx.loadFontFace` → 系统楷体族 → serif，已定降级链 | 前端 |
| R6 | 跑批与截单窗口服务不稳定 | 中 | 高 | 禁发窗口 + 任务幂等 + 手动补跑接口 + 监控告警 | 后端 |
| R7 | 口径在开发期间被临时改动 | 中 | 高 | 走 §六 变更流程；基线冻结清单已固定 SHA-256 | 产品 |
| R8 | 多会话 / 多协作者同时改同一文件互相覆盖 | 中 | 中 | 分支隔离 + PR + 禁直推 | 全员 |

---

## 八、M0 启动评审检查清单

开发启动前逐项确认。**阻塞 M1 的项全部 ✅ 即可开工**，非阻塞项滚动跟进：

| # | 检查项 | 状态 |
| --- | --- | --- |
| 1 | 《开发基线冻结清单 v1.0》已出，SHA-256 已记录 | ✅ |
| 2 | PRD v2.1 / ER v2.1 / 目录结构 v2.0 / 接口规范 v1.0 / 状态机 v1.0 全部归档 | ✅ |
| 3 | 设计 token 规范 v1.0 已定稿，`styles/tokens.scss` 与之一致 | ✅ |
| 4 | 协作规范 v1.0 已定稿，仓库 CI 已配好 | ✅ |
| 5 | 账号资源 11 项看板已核对（小程序 / 支付 / 域名 / 云资源） | ⏳ 你同步处理中 |
| 6 | 云资源（服务器 / MySQL8 / Redis7 / COS / HTTPS）已开通 | ⏳ 待开通（**阻塞 M1 建表**） |
| 7 | 《团长合作协议》简版文本（法务）已到位 | ⏳ 待法务（M2 前） |
| 8 | 供应商结算周期 ✅ **已定案＝日结**（C11）；对公账户信息**后置收集**（不阻塞开发） | ✅ / ⏳ |
| 9 | 团长佣金个税 ✅ **已定案＝灵活用工平台代扣**（C11） | ✅ |
| 10 | 种子数据已按《种子数据清单 v1.0》核对 | ✅ |
| 11 | **灵活用工平台**开户 + 服务协议 + 费率确认（C11） | ⏳ 待办（W1 D5） |
| 12 | `v1.0-baseline` tag 已打（commit `db91e16` · 326 文件） | ✅ |

> **M0 评审状态**：已执行（2026-09-14），结论 **通过** —— ✅ 7 项 / ⏳ 4 项（云资源 · 账号核对 · 团长协议 · 灵活用工开户），全部为**非阻塞或可滚动跟进**项。
> 评审纪要见《ABox一盒M0启动评审纪要v1.0.md》。

---

## 九、变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-09-14 | 首版：M1–M5 里程碑（含 M0 启动评审）+ W1–W10 甘特 + 验收标准 + 关键路径与缓冲 + 风险登记册 8 条 |
| v1.0.1 | 2026-09-14 | M0 启动评审已执行（结论通过）：§八 检查清单逐项核对（7 ✅ / 4 ⏳）+ 新增第 12 项（`v1.0-baseline` = commit `db91e16`） |
| v1.0.2 | 2026-09-15 | **M3 批次推进记录**：M3-1 后台鉴权基座（A2/A4/A5 + `AdminGuard` + 角色菜单 + 操作日志 · D51–D56）✅ · M3-2 套餐编排（D1–D7）✅ · M3-3 订单中心（D8–D12 + 退款执行引擎 + 反向结算）✅ · M3-4 退款审批（D40–D42 · C6 第二/三段收口）✅。每批次均以 `gate.mjs verify` 四段串跑（seed→m1→m2→m3）全绿为验收判据 |
| v1.0.3 | 2026-09-16 | **M3-5 后台团长管理（D19–D22 · 原型 P32）✅**：名录 / 申请流水 / 筛选器 / 详情（操作日志按**团长 id 与 用户 id 双键**查）· 任命与转交闸门（20012 附 `occupiedBy`，现任**停职而非删除**）· 改等级**同步写费率** · 停用/复职只走 D22 单一入口 · **两级权限白名单**（类级含 operator，D20/D21/D22 收窄到 super_admin/admin）· 错误码 20011–20013 · **零 DDL 变更**。`e2e-m3` 新增 §17（35 条断言，**不依赖下单窗口**）；`gate.mjs all` 11/11 ✅，`verify` 四段串跑须在**北京时间 14:00–23:00** 内复核（§15/§16 依赖真实下单） |
| v1.0.4 | 2026-09-16 | **M3-6 后台供应商管理 + 集散（D23–D32 · 原型 P33 / 模块 M34-01~04）✅**：名录与详情（**列表脱敏、详情才回真实手机号**；银行账号原文任何接口不回）· 新建即 `pending` 待审 · 资质审核 D26（与 `status` **正交**，驳回不自动下架）· 类型闸门 50008（纯出餐型不得挂集散中心，D27/D30/D31 三入口同拦）· 对公结算账户 D28 · 集散中心 D29–D32（软删两道前置，错误信息给「改用停用」的出路）· **两级权限白名单**（D24–D28 + 外卖链接 + 菜品写操作收窄到 super_admin/admin）· 错误码 50003/50005/50006/50007/50008 · **`ab_supplier` 补 7 列**（本批次唯一 DDL）。`e2e-m3` 新增 §18（35 条断言，**不依赖下单窗口**）；顺带修掉**根因级缺陷**：操作日志拦截器对**新建类接口**（请求里没有 id）补响应体兜底，否则日志永远 `targetId=null`。⚠️ 同批次修正一处**路径误占**：`/supplier/dishes`、`/supplier/edit` 原属商家端 P23/P24/P26，平台端 P33 改走 `dish-library` / `form` |
| v1.0.5 | 2026-09-16 | **M3-7 后台办公楼与楼群（D13–D18 · 原型 P37 五视图 / 模块 M33-01~02）✅**：① **状态三态修订** —— `ab_building.status` 由旧注释「1 合作中 / 2 停用」扩为 `BuildingStatus`（1 营业中 / 2 待开通 / 3 已暂停），修掉种子把「待开通」与「已暂停」都写成 `2` 的**一值两义**缺陷（楼群**刻意保持二态**）；② **覆盖缺口三因** `DistributionGap`（`none` / `no_group` / `no_center` / `all_center_disabled`）—— 三种成因三种修法；③ **集散映射全派生**（主/备集散 + 路线号 R1…Rn 由 `ab_distribution_center.service_groups` 实时派生，楼栋不存副本；**不返回距离/时长** —— 无地图数据，原型 km/分钟是演示值）；④ **D15 收口**：`buildingGroupId: null` = 移出楼群 · 空变更 10001 · **刻意不收 `leaderId`**（改团长只有 D20/D21 一个入口，避免绕过 20012 闸门）；⑤ **D18 fail-closed**：停用非空楼群 → 60003，且**先搬楼再判闸门**（一次请求内「清空成员 + 停用」放行）；⑥ **D14/D15/D17/D18 方法级白名单收窄**到 `super_admin` / `admin`；⑦ 错误码 60001–60005（新号段 `6xxxx 主数据`）；⑧ `ab_building` 增 `population` 列（本批次唯一 DDL，**无新表**，仍 25 张）。`e2e-m3` 新增 §19（**不依赖下单窗口**，整节独立块作用域防变量撞名）；同批次修掉**根因级缺陷**：`target: ES2022` ⇒ `useDefineForClassFields` 默认 `true` ⇒ DTO 的 `in` 判断**恒真**，导致「只改楼名」被误判成「移出楼群」（详见《缺陷与陷阱》真缺陷 #47） |
| v1.0.6 | 2026-09-16 | **M3-8 出餐确认（S1–S3 · 原型 P21/P22 / 模块 M21-01~03）✅**：① **S1 工作台** —— 出餐计划 + 按集散中心拆分的配送清单，`date` 缺省取「本主体最近一个有生产计划的出餐日」（不写死明日：周末/停团时最近有计划的可能不是明天）；② **S2 出餐确认** —— 粒度 = **(菜, 集散中心)** 逐项确认（与原型 P22 逐卡勾选一致）；**09:30 是 deadline 而非 earliest**（提前备好即可提前确认，迟于出餐日 09:30 一律 fail-closed `50009`）；**幂等**（重复提交进 `skipped`，不报错、不改时间戳）；`actualQuantity` 不传 = 足额送达，传了以申报值为准（短送留痕）；③ **S3 打包任务** —— 闸门 `ready` = 该中心当日菜品**全部确认送达**，否则列 `blockers`（未到齐就开包会包出缺菜的餐）；路线 `R1…Rn` 派生（同 D13 口径，站点按楼栋 id 升序）；非集散型主体 `visible=false` + reason（**HTTP 200**：「你没有这项任务」是正常状态不是错误）；**不返回距离与单段时长**；④ **新表 `ab_supplier_dish_center_daily`**（**25 → 26 张**，本批次唯一 DDL）—— 分中心确认是「一对多 + 事件语义」的数据：JSON 列既无法做「哪些集散中心还没确认」的聚合（S3 闸门刚需），也扛不住并发确认（丢更新）；⑤ **生产计划派生但落库** —— `ab_meal_assignment(status=active) × ab_set_meal_item` → 父表（日总量）+ 分中心明细；**惰性 ensure + 生成即冻结**（截单后改单不能让供应商的备料量在背后变化）；**只生成 `planQuantity > 0` 的行**（`sold_count=0` 的分配很常见，照单生成会让 P21 长出一串「0 份」的菜）；⑥ **父表状态由明细派生**（`pending` / `cooking` / `done`，**不新增 `partial`** —— 既有三值域已能表达，扩枚举会牵动 M1/M2 已验收的读端）；⑦ 错误码 `50009` `50010` `50011`；⑧ **种子修订** —— 4 家演示供应商补 `audit_status='approved'` + 证照有效期（M3-6 引入该列时种子未跟上，默认 `pending` 会让演示环境一进 P21 就被 50001 拦、整条出餐链走不通）；备选 6 家**刻意保持未登记**（用它们验证「未登记 ≠ 已过期」）；⑨ `e2e-m3` 新增 **§20**（34 条断言，**不依赖下单窗口** —— 超时用例用「昨日」构造，与运行时刻无关）；同批次修掉**根因级缺陷**：**同一轮对同一文件发多个 Edit 会互相覆盖**（后写基于原始快照，前写丢失）—— 本轮即因此丢过 `forFeature` 注册与 2 家供应商的种子字段（详见《缺陷与陷阱》真缺陷 #48） |
| v1.0.7 | 2026-09-16 | **结算口径改自营（用户逐条确认 4 项）✅**：产出 **《ABox一盒自营结算口径定义v1.0.md》** —— ① ⭐ **用户退款不冲减供应商应付**（半成品出餐日已交付；`type='reversal'` 改义为「应付单算错」的**纠错冲销**，不再由退款触发）→ **须回退 M3-3 的 `reverseSupplierShares`，列为 M3-9 开工前置**（佣金冲销与余额回退**不受影响**）；② **计费基数 = 实收量**（`ab_supplier_dish_daily.actual_quantity`；S2 未申报则 = 计划量），单价 = `ab_dish.cost_price` —— **不是订单销量**，采购的本质是「收多少付多少」；③ **应付对象只剩供应商采购款**（B2B），场地摊销 / 打包人工 / 配送费转为 **ABox 自身履约成本**（**不出付款单**、只进成本核算），`payee_type='distribution_center'` **冻结**；④ `siteFee` **保留科目改语义** → 「**ABox 自有持证场所摊销**」，**默认值不再为 0**；⑤ **不变量 I1** —— 供应商端**禁止出现**终端售价 / 佣金 / 毛利 / 成本合计（B2B 只该看到「我的价 × 我的量」）；⑥ 中文口径「平台毛利」改称「**经营毛利**」（字段名不动），并明确：**履约成本未登记时该值只是上限、会被系统性高估**（D47–D50 看板须显式提示）；⑦ S9 出单 **fail-closed** —— 父表 `status != 'done'` 时 **不出单**、进「未出单异常清单」（此时 `actual_quantity` 只覆盖已确认明细，按它出单会少付、按计划补齐会多付）+ 幂等键 `(supplier, dish, meal_date)`；⑧ 错误码预留 `50012` / `50013` / `50014`；⑨ 同步回写：《接口规范》§五 S9 口径整表重写 + 退款冲销段作废标注 + C4 / D11 / 费用默认值 / 毛利公式四处修订、《ER v2.1》§5.2 `ab_supplier_share` 行作废标注 + `type` 字段改义、《资质与合规》§4.3、`biz.ts` / `finance.entity.ts` / `reversal.service.ts` 三处源码注释（**本批零逻辑改动、零 DDL**） |
| v1.0.8 | 2026-09-16 | **M3-9 应付结算 S9 落地（自营口径首次实装）✅**：① **回退 M3-3 `reverseSupplierShares`**（开工前置已执行）—— 退款**不再冲减**供应商应付；改为显式声明 `supplierShareAdjusted=0` + `supplierShareMode='not_applicable'`（**保留字段而非删掉**，让「应付分文未动」成为一条可断言的事实），`type='reversal'` 仅用于「应付单算错」的纠错冲销；② **出单** `POST /admin/supplier-shares/generate` —— 计费基数 = **实收量**（`NULL` 视为足额，与 S2「不传即足额」同语义）；⭐ **单价取 `ab_supplier_dish_daily.unit_price`（生成即冻结的协商价快照）**，为空才回落 `ab_dish.cost_price`，**并据此修正了口径文档 §4.1 的旧写法**（若恒取当前成本价，就会出现「T 日按旧价交货、结算按新价付」，供应商对账必然拒绝）；③ **fail-closed 四闸**：资质异常（与 S2 的 50001 同判据同顺序）/ 父行 `status != 'done'` / 实收为空 / 实收 0 → 一律**不出单**并进「未出单异常清单」，逐条给原因与出路；**补齐输入重跑即可补出**先前被拦的单（出路是「补输入再跑」，不是人工绕过）；④ **付款登记** `POST /admin/supplier-shares/{id}/payment` —— 仅 `pending` 可登记（否则 `50012`）、回单号必填（否则 `50013`）、同一回单号不得用于两笔（两笔支出挂同一凭证，对账分不清）；`canRegisterPayment` + `blockReason` 由服务端下发；⑤ **幂等走软层**（同一 `(供应商, 菜品, 出餐日)` 已有有效 `normal` 行即跳过）—— **刻意不建 DB 唯一索引**：本表还要容纳 `type='reversal'` 负行，同键正负两行是**合法冲销**，唯一索引会误伤；⑥ **未出单异常清单** `GET .../exceptions?date=`（`date` **必填** ——「历史上所有没出单的原因」不是一份可执行的清单）；⑦ **供应商端 S9 自查** `GET /supplier/settlement` —— 复用财务侧 `SupplierShareService.supplierView()`，**运营看到的数与供应商看到的数只有一份实现**；出参含 `pendingAmountFen`（该日）与 `pendingTotalAmountFen` + `pendingTotalRowCount`（**不限日期**的待付合计，供应商真正关心的是「平台还欠我多少」）；**不变量 I1 落在数据结构层**（出参里根本没有售价/佣金/毛利/成本字段，不靠前端隐藏）；⑧ 跑批 `SupplierShareTask`（**T+1 02:00**）与运营手动补跑**共用同一执行口**；⑨ **权限两级白名单**（类级含 `operator`：要跟进「为什么没出单」；`generate`/`payment` 方法级收窄到 `admin`/`finance`：决定「欠多少」「付没付」是资金动作）；⑩ 错误码 `50012` `50013`（**`50014` 取消**：「确认未完成 → 不出单」是常态待办、不是失败；做成错误码只会让运营看到「出单失败」却看不到「哪几家没确认」，号位已释放）；⑪ `e2e-m3` 新增 **§21**（41 条断言，**不依赖下单窗口**，夹具全部自造）；⑫ **顺手清掉一批旧语境**：供应商端页面仍写着「分账单价」（`packing.vue` / `workbench.vue`）、集散页标题「复用合作供应商场地」、`seed` 的 `settlement.site_fee` 文案、`finance.entity` 的列注释 —— 自营下这些字样与口径矛盾，留着就是「两个真相」；⑬ 同批次修掉两处 **e2e 基础设施型假红**：`node:sqlite` 的 `busy_timeout` 默认 0（服务端持写事务时夹具直连读会抛 `database is locked`，把一次撞车放大成整节中断）、decimal 列经 SQLite **NUMERIC 亲和性**回读成数值（`'750.00'` → `750`，字符串比较必假）—— **另记录一条流程教训**：本轮先单跑 `e2e:m3` 未先 `seed`，被上一轮残留的订单/团长状态带出一整节崩溃（`waitDb` 拿到 `undefined` 参数），纪律早已写在《缺陷与陷阱》§三，属**未按纪律执行**而非新缺陷 |
| v1.0.9 | 2026-09-16 | **M3-10 系统配置 D57–D58 落地（P36 · 零 DDL · 零错误码）✅**：① ⭐ **本批次最大产出是「如实标注未接线」** —— 逐键排查 `ab_config` 全部 30 项配置的**真实消费点**，发现 **9 项「种子里配了、服务端代码从不读取」**：`set_meal.publish_time` / `cutoff_time` / `delivery_arrival_time`、`commission.auto_confirm_time` / `settle_hour`（**实际调度硬编码在 `@Cron()` 装饰器**里 —— NestJS 的 cron 是静态元数据，不读配置）、`distribution_center.default_count` / `rice_fee` / `pack_fee`、`supplier.settle_cycle`（口径已改表驱动或代码常量）。这些键**继续展示但置为只读 + 点明原因**（`unwiredReason` 直指是哪个 task 硬编码），而不是给一个「看起来能改、改完没反应」的输入框 —— **让运营改一个不生效的值，比不给他改更糟**；另 2 项为**策略标识**（`negotiated` / `residual`），存的是策略名，塞金额进去会污染口径记录；② **`CONFIG_SPECS` 单一真相**：一份声明同时驱动 D57 分组/文案、D58 白名单/取值范围、前端控件类型（标签写前端 + 校验写 DTO + 可写判定藏服务方法 = 同一个键三份定义互相漂移）；③ **写入三条纪律**：**白名单**（未知键 → `10001` 而非静默忽略；未接线项与策略标识**一律拒写**）、**整批原子**（任一项不合法 → 整批不写入，部分成功会让「二次确认」失去意义）、⭐ **写完同步 `invalidate()`**（`BizConfigService` 有 60s 进程内缓存，等 TTL 就会出现「配置页已改、业务按旧值跑」）；④ ⭐ **percent 单点换算**防 100 倍错误：入参/出参都是百分数（`8` = 8%）、库内存 `0.0800`，换算只在 `normalizeForStore` 一处（e2e 用「写 8.5 → 库内必须 0.0850」钉住）；⑤ **数值格式前置校验**：`Number('')` 是 0，不先过正则就会把「清空金额框」静默存成 `0.00` —— 对成本项就是「悄悄变回未登记」且毫无提示；⑥ **库中缺失键的处理**：`order.pay_timeout_minutes` / `settlement.supplier_total_default` 不在种子里、靠代码兜底运行 → D57 显示 `valueSource='fallback'` + 兜底值（空白会让运营以为「配置丢了」，而系统其实正按该值在跑），D58 首次调整时 **INSERT** 新行（UPDATE 不到就失败 = 这个值永远改不了）；⑦ **履约成本登记判据单点化**：`isCostRegistered()`（值 > 0）落 `shared-utils`，**D57 配置页与 D47 看板共用**（否则出现「配置页说已登记、看板说未登记」）；D57 的 `meta` 刻意走 `BizConfigService.settlementCostState()`（**经缓存**），使「写完即时生效」可被 e2e **真实验证**（断言「写 `site_fee` 后立刻重读 D57 应看到已登记」）；⑧ **无新增错误码**（复用 `10001` + `data.fields` 逐条中文问题）；⑨ e2e 新增 **§22（35 条断言 · 不依赖下单窗口 · 夹具节末全量还原** —— 配置是**全局**的，不还原会把「费率 8.5」这类副作用留给重跑）；⑩ **顺手校正三处文档与代码不符**：`admin.module.ts` 落点清单（`finance-admin.controller.ts` 实为 `refund-admin` / `supplier-share-admin`，并补记 D33–D35 / D38–D39 / D43–D44 / D59–D60「**待落点**」）、§八 映射表 P34/P35/P36 的实装进度、`config.specs.ts` 内 `@Cron` 字面量改用双引号（1 处漏转义曾致 TS1005）；⑪ ⚠️ **登记一项独立待办**：时间类配置（截单 / 开团 / 自动确认 / 结算时点）与调度**分家** —— 配置写 23:59、实际由 cron 决定、下单窗口实为 `[14:00, 23:00)`，**三者对不齐**；动态化需改 `SchedulerRegistry` 或 `@Interval`，会动到**下单窗口**（e2e 全量依赖 14:00–23:00），属独立验收项（详见《缺陷与陷阱》**#49**） |
| v1.1.0 | 2026-09-16 | **M3-11 数据看板 D47–D50 落地（P35 · M36 · 零 DDL · 零错误码）✅**：① ⭐ **口径唯一真相 `stats.constants.ts`** —— 状态集合 / 区间档位 / 基数选择 / `topN` 上限 / cohort 周数**全部收一处**。看板的每个数都是「多表 + 一组状态 + 一条时间轴」聚合出来的，而同一指标极易在两个端点各算一套（D47 的 GMV 与 D48 的楼群 GMV、D49 的菜品份数），一旦漂移就是「总额 ¥4,798、分项加起来 ¥4,301」—— e2e 直接断言 `D48.totalGmvFen === D47.metrics.gmvFen` 把它钉住；② ⭐ **修正《接口规范》§6.6 的 GMV 口径**：原写「已完成订单、不含已退款」，实装改为「状态 ∉ {未支付, 已取消, 已退款}，**含在途退款**（`refund_applying` / `refunding`）」—— 在途退款的钱还没退、佣金也尚未冲销，剔除会造成「钱已收但 GMV 不计、佣金却还在支出」的**双向错配**。四个减项必须放进**同一个等式**才算得平（e2e 断言 `grossProfitFen === gmvFen − purchaseFen − fulfillmentFen − commissionFen`）；③ **成本侧一律取净额**：采购款剔 `status ∈ {cancelled, reversed}`（`reversal` 负行**必须计入** —— 它自己 status 有效，只取正行会把已纠错的应付又算一遍）、佣金剔 `status='cancelled'`；④ **履约成本 = 三项配置单价之和 × 份数**（`settlement.site_fee` / `packing_labor_fee` / `delivery_fee`），**不含** `supplier_total_default`（种子兜底示例值；采购款走实际应付单，两者都扣即重复计成本）—— 这是 M3-10 落地的三项配置的**首个真消费者**；⑤ ⭐ **毛利可靠性提示**：履约成本未登记（复用 `summarizeCostRegistration()`，与 P36 配置页**同一份实现**）或采购应付单未生成 → D47 出 `warnings`「上限值」提示。任何一个减项缺失都会让经营毛利虚高，不给提示等于让运营拿虚高的数做决策；⑥ **经营毛利可为负是刻意保留的信号**：退款订单的采购款仍要付（自营口径「退款不冲减供应商应付」），故该部分毛利为负，前端 `is-negative` 标红；⑦ **区间只开 `today/7d/30d` 三档**、缺省 `7d`、**含末日**（近 7 日 = [今日−6, 今日]），**非法 `range` → `10001` 而非静默回落**（否则运营以为在看 90 天、实际看的是 7 天）；统计基准 = **出餐日**（不是下单时刻 —— 跨日下单按下单时刻归集会落到错误的那一天）；`trend` 区间内每天都给一行、**Σ 逐日 GMV = 区间 GMV**；⑧ ⭐ **D49 按菜品合并**：一个菜被多个套餐引用时**必须并到同一行**（本库 `m1 = 菜品 1/6/4/7`、`m2 = 2/6/4/7`，6/4/7 为共享菜）—— 按套餐展开就会拆成两行、热度被腰斩；占比分母 = 区间内**全部**份数而非 `topN` 之和（否则排行末位占比凭空虚高），`topN` 上限 50；⑨ ⭐ **D50 留存数字成对下发**：`retainedWeek1` 与 `retentionRate1` 只在「观察窗口（首单周 +7…+13）已走完 **且** 该群非空」时才有值，否则双双 `null` —— 未走完时算出的低留存只是「数据还没长出来」，0 人分群则是 `0/0`；**两种 `null` 语义不可混用**（`observable=false` = 观察中，`observable=true` + 率 null = 空群），混成一个就把「没人来」错说成「数据还没长出来」；新客判定看**全历史**首单（只看区间内的话所有人都会显得像新客）、cohort 按**自然周（周一为始）**而非 4 个 7 天窗口（后者与周报对不上）；⑩ ⭐ **权限白名单必须含 `viewer`** —— `admin-role.ts` 里 `viewer` 的菜单**只有这 4 个看板页**，接口漏了他就等于「菜单能点、点了报 10003」，最容易被当成 bug 的一类不一致；`operator`（跟进经营数据）与 `finance`（核毛利）同样放行，双主体隔离照旧（小程序/供应商 token → `10003`，不变量 I1 在看板上同样成立）；⑪ **实现选型：取行回内存聚合、不下推 SQL 求和** —— `SUM(decimal)` 在 MySQL 返回字符串、SQLite 返回浮点，而四驱动零改动切换是硬约束（`fen()` 归一到整数分才能跨驱动逐分一致），且「有效订单」判定必须三处同源；代价 O(行数)，**单量破万后再下推（届时只改 `stats.service.ts` 一个文件）**；⑫ **顺手清掉三处「两个真相」**：`stats.constants.ts` 删掉两个**从未被引用**的常量（`STATS_EXCLUDED_STATUSES`、`REFUND_RATE_EXCLUDED_STATUSES` —— 后者注释描述的是「按 `ab_refund` 算」这套**没有采用**的算法，留着会让后来者照它实现出第二套口径）+ 文件头引用的旧 §6.6 文案同步更新 + `core-metrics.vue` 的 GMV 副标题由「不含已取消 / 已退款」（漏了未支付、且与「在途退款计入」矛盾）改为「不含未支付 / 已取消 / 已退款（**在途退款计入**）」并在两处互指明「端上文案是口径的投影」；⑬ **e2e 新增 §23（61 条断言 · 不依赖下单窗口 · 夹具节末全量还原）**，用**增量对照 + 结构不变量**而非写死累计值 —— 看板是**全局**聚合，不还原会把「凭空多出的 GMV / 佣金 / 采购款」留给后续重跑；⑭ **顺带修掉一条「测试写错、代码没错」的假红**：D49 断言原把共享菜当独占菜写死 `+5 份`（实测 `#4`/`#6` 应为 `8 份`），改为**从 `ab_set_meal_item` 推导期望值** —— 断言写死数字等于把「数据形状的巧合」当成契约 |
| v1.1.1 | 2026-09-16 | **M3-12 通知模板 D59–D60 落地（P36 · 新增第 27 张表 `ab_message_template` · 零错误码）✅**：① ⭐ **存储选型：新建独立表，而不是塞进 `ab_config`** —— `CONFIG_SPECS` 的类型系统是**扁平标量**（money / percent / int / text / time），撑不住「场景 + 多渠道 + 变量白名单 + 启用闸门」；硬塞只有两条路：JSON-in-`config_value`（则 D58 的白名单与单点校验失效 → 立刻多出第二个真相），或拆成十几个扁平键（则「一个场景」这个整体在库里不存在）。⭐ 另一处**规格纠偏**：规范 §八 原把 `ab_message` 列为 P36 依赖 —— 实查它是**推送日志**表（`template_id` 存的是微信侧模板 ID），**不是模板定义**；故新增第 27 张表并在此登记；② ⭐ **场景定义是代码事实，不落库**：`scene` / 渠道组合 / 触发时机 / 变量白名单 / 是否必推全部收在 `message-template.specs.ts`（原型 P36 卡片 5 行逐行对应）；库表只存**可编辑部分**（`enabled` / `wechatTemplateId` / `groupContent` / `updatedBy`）。落库会漂移成「库里写着走微信群、代码只发订阅消息」这种**无法自证**的矛盾；③ ⭐⭐ **启用闸门 fail-closed**：每个渠道有**必要条件**（`wechat_subscribe` → 模板 ID；`wechat_group` → 文案），缺一即**拒绝启用**（`10001`）—— 放行的后果是页面显示「已启用」而投递必然失败，用户永远静默收不到且**没有任何地方显示异常**；判定按**改动后的最终状态**（不是只看本次提交的字段），否则可「先清空模板 ID，再单独发一次 `enabled=1`」绕过；判定函数 `missingForEnable()` 落在 `specs`，**管理侧（保存前）与投递侧（能否发）共用**；④ ⭐ **接线状态两级如实标注**：**场景级** `wiring`（`live` = 有真实投递点 / `pending` = 一期无，**必附 `pendingReason`**）+ **字段级** `fieldWiring`（`enabled`/`wechatTemplateId` = `live`；`groupContent` = `record_only` —— 供人工复制发群，**不是程序行为**）。一期 `live` 仅 `refund_result` 一条；⑤ ⭐⭐ **明写「微信订阅消息的文案由微信公众平台侧定义」**：订阅消息内容在公众平台按模板 keyword（`thing1`/`time2`）定义，服务端只能往 `data` 填值 —— 页面 `note` 直接讲清「改本页文案**不会**改变用户收到的订阅消息」，否则运营会以为改了文案用户就能看到新内容（M3-10「给了输入框却没接上线」的同类）；⑥ **变量白名单 + 边界**：`{{变量}}` 必须 ∈ 该场景 `variables`，否则 `10001`（写错的变量发送时不被替换，用户直接看到 `{{xxx}}` 原文）；**只有双花括号算占位符**，单花括号 `{3}` 不匹配（否则「今日共 {12} 份」这类自然文本会被误判拒写）；⑦ **只读字段拒绝而非静默忽略**：全局 `ValidationPipe` 已开 `forbidNonWhitelisted` → DTO 未声明的 `scene` / `channels` 直接 `10001`（**静默忽略更糟：调用方以为改成功了**）；**「未传」与「传 null」是两种语义**（缺省 = 不改、显式 `null` = 清空），判据 `!== undefined`（见《缺陷与陷阱》#46）；空请求 → `10001`；⑧ ⭐ **真消费点 = 退款结果通知**：`refund.service.ts` 的**两个执行口**（D11 强制退款 / D41 审批通过）在 **`dataSource.transaction()` 返回之后**调 `MessageService.notify()` —— ⭐ **必须在事务外**：事务里发了通知而事务回滚，就是「用户收到退款通知但退款并未发生」。`notify()` **不抛异常**，问题全部收敛进返回值，调用点再兜一层 `try`（退款已成功、钱已出去，通知失败只能是「这次没通知到」）；`userId → openid` 换算归投递侧（业务侧不该为发通知去 join 用户表）；⑨ **投递侧实装原空模块**：`message.service.ts` 原为 `export class MessageService {}` 的零实现占位，本批次实装；同时**删除 `wechat-template.service.ts` 与 `templates/index.ts` 两个零引用死占位**（「模板定义该写哪」由此**只有一个答案**）；⑩ **未启用 = 不投递且不留日志**：`ab_message` 的语义是「**发过什么**」，把「因为没启用所以没发」也写进去，这张表就失去查询价值（e2e 用「关闭场景退款 → 日志增量 0；启用后同操作 → +1 且 `status=success` 且 `template_id` = 后台刚配置的值」把这条钉死 —— 配置**真的被用上了**，不是代码里的常量）；⑪ **不引入缓存**：全表 5 行、只在「打开管理页」与「产生一条退款通知」时读，属低频路径；**没有缓存就没有「写完忘了失效」**（将来读频率上来再加，届时必须同时补 `invalidate()` 调用点，代码注释已留此约束）；⑫ **种子由 `MESSAGE_TEMPLATE_SPECS` 派生**（不存在第二份场景列表）：只有 `leader_delivery` 启用（含微信群渠道，人工发群只需文案，不依赖尚未申请的微信模板 ID），其余 4 个 `enabled=0`，如实反映「一期没有微信账号」；库中「有行但声明里没有」的场景**不展示 + 大声记 WARN**；⑬ **错误码零新增**（闸门 / 变量 / 只读字段 / 空请求 → `10001` 且 `data.fields` 回带逐条中文说明；目标不存在 → `10004`）；权限沿用类级 `super_admin`/`admin`，`operator`/`finance`/`viewer`/`supplier`/小程序 token → `10003`、未登录 → `10002`；`@OperationLog({module:'system',action:'编辑通知模板'})`（必须能回答「谁在什么时候关掉了退款通知」）；⑭ **e2e 新增 §24（47 条断言 · 不依赖下单窗口 · 夹具逐字段还原）**；⑮ ⚠️ **本批次自测抓到一处设计不自洽（已修为真缺陷 #50）**：原型把「团长送达通知」写作**双渠道**（微信群 + 服务通知），照抄进 `specs` 后该场景在种子里是启用态、却必然缺订阅消息模板 ID → **「已启用」与「启用闸门」互相矛盾**，首跑 6 条红（位置分散、根因同一）。修法：`channels` 改**单渠道 `['wechat_group']`** —— **渠道是代码事实，代码一期只发人工群就照实写**（二期接服务通知时改 `specs` 加渠道即可，闸门会自动开始要求模板 ID）；⑯ ⚠️ 同批次另登记 3 条**测试侧**教训进《缺陷与陷阱》§三（独立块作用域导致跨节 `const` 不可见 → 整节 `ReferenceError`；幂等断言须用**当前值**而非本节快照，并补「不同 → 恰好 1 项」的反面对照；出参是视图对象 `{list,summary,note}` 不是数组）；⑰ ⚠️ **收尾 `verify` 抓到并修掉一条「顺序脆弱型假红」**：§23 原写 `ΔactiveLeaderCount === 1` / `ΔactiveBuildingCount === 2`，而 Δ 隐含「这些主体在本节之前**不活跃**」—— e2e:m1/m2 与 §15/§16 会在**同一 7d 区间**留下**同一** leader / 楼栋的有效单（实测留有一单 `refund_applying` · `team_leader_id=1` · `building_id=1` · 出餐日 = 今天），`before` 基线已含该主体 → Δ 由 1/2 变成 0/1。症状极典型：**`seed → e2e:m3` 单跑全绿、`verify` 串跑两条红**，且失败点与被测产物完全无关（SQL 独立算一遍，产品代码没错）。**修法**：改为**库内对照 oracle**（同口径 SQL 独立算一遍，断言 `api === oracle`，与基线无关）+ ⭐ 补一条「本节夹具自身贡献 2 栋不同楼栋 / 1 名团长」的**结构断言**（否则等式两边同为 0 也能过 —— 只换 oracle 不补这条等于把断言改空）。⚠️ **纪律升级：「增量对照」不是万能 —— 对『多章节共享的主体』天然失效**（用户维度可 Δ：夹具取「从未下过单的用户」；共享维度不可 Δ）；`verify` 四段串跑全绿（m1 38 · m2 121 · **m3 675**） |
| v1.1.2 | 2026-09-16 | **M3-13 财务端点 D33–D35 落地（P34 · 模块 M35-01/02 · 零 DDL · 零错误码）✅**：① ⭐⭐ **D33 不自己算一套口径** —— 收入 / 成本 / 履约 / 毛利 / 逐日趋势**全部取 `StatsService.dashboard()`**（与 D47 看板**同一函数**），「财务页的 GMV」与「看板的 GMV」在结构上不可能漂移（e2e 逐项相等钉死）；② ⭐ **时点量 vs 区间量**：`liability`（余额 / 冻结 / 待入账佣金）**不随 `range` 变化**、出参带 `asOf`（时点量被误读成「本期新增负债」是最容易误导人的一类错）；③ `range` 复用 `STATS_RANGES`，`date` 作**区间终点锚点**（期末对账要看已经过完的那一天），该入参由 `StatsQueryDto` 承载、**D47–D50 同时获得**；④ ⭐⭐ D34 `rate` / `leaderLevel` 是**结算快照**（C2：团长晋级后历史行仍显示当时等级与费率，列头标「（快照）」）；⑤ D34 扩展入参 `status` / `type` / `keyword`（非法枚举 `10001`，不静默回落成「全部」）；⑥ ⭐⭐ **D35 = M4 `commission-settle.task`（T+1 02:00）的同一执行口**（跑批上线只需把 `@Cron` 接到 `settlePending`，不另写第二套入账逻辑）；⑦ ⭐ **一期 `pending` 常态为 0** —— 佣金在 L9 取餐确认时**即时入账**，故 D35 出参必带 `note`说明「这不是故障、也不是钱没结」；⑧ ⭐ **D35 刻意不做「补计佣」**（`ab_order` 没有下单 / 确认时刻的团长等级快照 → 任何事后补计佣都只能用**当前**等级，晋级后会多算；**结构性缺口**已登记）；⑨ ⭐⭐ **补账只改账、不改事实**（只累加 `total_commission`，**不动** `total_orders` / `last_order_at`，否则污染 C2 晋级审计 —— 余额对了、佣金对了，只有团长的晋级进度被悄悄推快）；⑩ D35 **整批单事务** + 逐行 `affected` 判归属（并发不重复加钱）+ 无归属进 `skippedReasons`（不猜、不建号、不静默丢）+ 入参**不收金额**（否则就是往团长余额加钱的后门）；⑪ 权限两级（类级含 `operator`，D35 收窄到 `admin` / `finance`；**不含 `viewer`**，与 D47–D50 刻意含 viewer 正好相反）；⑫ ⚠️ **顺手清掉死占位** `modules/finance/finance.controller.ts`（原空类、零引用），「财务端点该写哪」由此只有一个答案；⑬ e2e `§25` 新增 **33 条断言**（**不依赖下单窗口**，订单与佣金夹具全部直插） |
| v1.1.3 | 2026-09-16 | **M3-14 余额账户管理与调整 D38–D39 落地（P34 · 模块 M35-04 · 零 DDL）✅**：① ⭐⭐ **负债合计不自己算** —— D38 的 `liability` 直接调 `FinanceService.loadLiability()`（**由 `private` 改 `public`**，与 D33 资金总览**同一个函数**）；两页各写一套 `SUM(ab_balance)` 必然漂移且**没有任何报错**，只会让运营在两页看到两个不同的「平台欠用户多少钱」（e2e 写前写后各钉一次 `D38 ≡ D33`）；② ⭐ **负债是时点量**：`pageSize` / `accountType` / `keyword` 都**不改变**它（`total` 才是本次筛选命中数）—— 把 `liability` 做成「当前页求和」是最容易犯的错；③ ⭐ **D39 补第四动作 `unfreeze`**：规范原只写「充值 / 扣减 / 冻结」，但**不能解冻的冻结是死钱**；④ ⭐ **冻结 / 解冻不动 `total_in` / `total_out`**（钱没进出平台，只是从「可用」挪到「冻结」；与 L12 提现「申请阶段不计入累计支出」同口径）；⑤ ⭐ **余额不得为负**：扣减 / 冻结越界 → `40002`（钱不够花）；解冻越界 → **新码 `40015`**（**冻结账对不上**，是账实不符信号，要查数据而非让人充钱）—— **两码刻意分开**（合流一个码，运维就分不清该做什么）；⑥ ⭐ **幂等键必填**：调账没有业务单号，重复提交就是重复加钱（缺键 `10001`；命中 `10006` + **首次结果原样返回**，余额一分不再动）；⑦ **乐观锁** `UPDATE ... WHERE id=? AND version=?`（并发调账不丢更新，以 `affected` 判归属）；⑧ ⭐ **只有「充值」能自动建户**（其余动作在无账户时**明确报错**，**不是**先建 0 余额账户再报余额不足 —— 否则库里凭空多一行「余额 0」的账户，让 D38 账户数与 D33 `accountCount` 悄悄错开）；⑨ ⭐⭐ **权限常量单一真相**：`BALANCE_ADJUST_ROLES` **同时**驱动 `@Roles()` 与出参 `actions.canAdjust`，结构上不可能出现「按钮亮着、点了 `10003`」（项目既有写法多为两处硬编码 + 注释对齐，本批改为常量）；⑩ **`ab_balance_log.type` 新枚举值 `'adjust'`**（与原五值 `commission` / `order_pay` / `withdraw` / `withdraw_refund` / `refund` **刻意分开**，避免污染佣金汇总；同步补 `BALANCE_LOG_TYPE_LABEL`，否则用户在自己的余额明细里会看到裸英文）；⑪ 未知 `action` 走 **fail-closed `10001`** 而非静默走完 `switch` —— 否则会写出「余额没变却多一条流水」的脏数据（余额看着对、只有账本对不上，最难查的一类）；⑫ 溢出**前置挡**（`ab_balance` 四列 `DECIMAL(12,2)`：不挡就由 DB 抛**驱动相关**错误，且**同事务流水尚未写**、运维无法定位是哪一步）；⑬ **D38 允许「无账户」空视图**（`hasAccount=false` + 最近 20 条流水）—— 否则运营搜不到人 → 以为查无此用户 → **不敢充值**，而 D39 恰恰支持首充建户；⑭ 筛选走 **SQL 子查询**（避开 SQLite 绑定变量上限 999，M3-9 已踩过）；⑮ 前端 `balance.vue`（负债卡片 + 筛选 + 单用户账户卡 + 最近流水 + 调账弹窗，元→分换算预览、幂等键每次打开生成且失败不重置）；⑯ ⚠️ 顺手修掉**前端菜单缺口** —— `ADMIN_NAV` 财务域原只挂 `/finance/overview` 一个入口，其余 5 个子页（含本批余额页）服务端早已授权却**点不到**，运营只能手输 URL；⑰ e2e `§26` 新增 **34 条断言**（**不依赖下单窗口**，夹具直插新用户、节末全量还原）（⚠️ 本行同时**补回 M3-13 缺失的 v1.1.2 修订行**：该批把版本号改到 v1.1.2，但修订表未同步） |

| v1.1.4 | 2026-09-16 | **M3-15 对账 D43 + 发票 D44 落地（P34 · 模块 M35-06/07 · 零 DDL · 零新增错误码）✅**：① ⭐⭐ **一期不许假装已与微信对平** —— 无商户号 + 无 API 证书即**拿不到微信账单**，出参强制带 `channel.source='local_only'` + `billAvailable=false` + note 明写「**不等于已与微信侧对平**」；若报成「已对平」，真正的差异（微信收了钱、系统不知道）将**永远不可见**（端上 `channel.note` 常驻、不折叠）；② ⭐⭐ `date` 锚 = **支付日**（`anchor='paidAt'` + `anchorLabel='支付日'`）—— **财务域唯一一个 `date` 不指出餐日的端点**（微信账单按支付日切日），这是与 D33/D34/D36 的**有意偏离**，故显式回显，否则运营会拿它对出餐日数字并得出「对账对不上」的错误结论；③ ⭐ 出参主体是**差异清单**而非流水（当天进出账去 D33/D40 看），五类差异各带 `nextAction`（人话的下一步）；④ ⭐ **三角恒等式** `diffFen === orderFen − logFen`（正常为 0）+ 退款单列 `refundFen` / `netFen`；⑤ ⭐⭐ `balanced` **双条件**（金额相等 **且** 无结构差异）—— 重复/缺交易号可能不影响合计金额，**只比金额会报「已平」**，而凭证重复正是重复入账的前兆；⑥ ⭐ 差异检测**三个方向**：订单→流水（含「流水停在 pending」）· 当日订单的**全部**流水（**不限日期**，否则回调丢失时 `paid_at` 为空的流水会被日期切掉，恰好漏掉最该抓的一类）· 当日成功流水→订单 + 重复交易号；⑦ ⭐ **刻意不提供「一键平账」**：对账的作用是暴露差异，不是把差异抹掉（无写动作、无 `force` 参数）；⑧ ⭐⭐ D44 **零 DDL · 派生视图** —— 不建 `ab_invoice`（事实已在 `ab_supplier_share`），建表立刻产生**第二份真相**（「应付表说付了、发票表说没票」以谁为准）；⑨ ⭐⭐ 粒度 = **供应商 × 月份** —— 按单条应付行平铺会看到「同一发票号重复 30 次」，**完全看不出**「这家这个月只开了一半」，故三态中 **`partial` 是本页存在的理由**；⑩ ⭐ 月份锚 = **应付单生成月**（`share_date`，权责发生制台账，跨月付款仍归原月），复用 `monthRangeOf()`；⑪ ⭐⭐ 开票分母**只含已付款行**（未付款就要票供应商不会给），未付款额单列 `unpaidAmountFen`，**不进开票状态判定** —— 否则刚出单的日子会满屏「未开票」，把真正的欠票淹没；⑫ ⭐ 两个**可执行的下一步**：`titleMissing`（去 D28 补抬头，没抬头票开不出来）· `overdue` + `overdueDays`（已付超 30 天无票 = **税前扣除凭证缺失**的税务风险）；⑬ ⭐ 纠错冲销行**不进发票分母**，只累加 `reversalFen` 提示需**另行换票**（本期不做换票流程，边界写进 `note`）；⑭ ⭐ 分月**必须在服务端内存**完成（`strftime` / `DATE_FORMAT` 是**驱动相关**语法，用了就破坏「四驱动零改动切换」，本地全绿、换驱动才炸 —— 同 M3-6/D29 楼群筛选的既有处理）；⑮ `status` 是**派生值** → 必须**先聚合再筛**，非法枚举 `10001`（**不静默回落成「全部」**，回落会让运营漏催）；⑯ 两接口均为**纯读 GET** → **不额外收窄**权限（类级含 `operator` 即可），`/finance/invoices` 同时进 `ADMIN_MENU_KEYS` + finance 角色菜单 + `ADMIN_NAV`（**授权了就必须有入口**）；⑰ ⚠️ 顺手删掉**死占位** `reconciliation.service.ts`（原 `export {}` 骨架）—— 同 M3-13 删 `finance.controller.ts`、M3-12 删 `MessageService` 空占位；⑱ e2e `§27` 新增 **34 条断言**（**不依赖下单窗口**，夹具直插 + **隔离支付日** 取绝对值 + 节末全量还原：应付单不删会让下轮三种开票状态**互相串味**） |

| v1.1.5 | 2026-09-16 | **M4-0 契约级修正（自营口径前置 · 零新增列 / 零新表）✅**：① **场所归位** —— `ab_distribution_center.supplier_id`（原 NOT NULL）改**可空历史字段**（列保留 · 新逻辑不读不写）；对外**派生文案**统一称**「加工场所」**（实体名与库内 `name` 保留）；② **供应商类型停用** —— `ab_supplier.type`（`dish`/`distribute`/`both`）同模式转历史字段，D23 列表**不再下发 `typeOptions`**、`?type=` 不再受理（传即 `10001`），详情由 7 块减为 **6 块**（去「加工场所」块与 `dcCount`）；③ ⭐ **D27 `PUT /admin/suppliers/{id}/type` 整条路由删除**（→ `10004`），`50008 SUPPLIER_TYPE_CONFLICT` **三处闸门全拆、号位保留不用**；D29/D30/D31 三入口**均停收 `supplierId`**（→ `10001`，属「请求形状错误」而非业务值非法）；④ ⚠️ **第 4 处真风险（本轮盘出 · 计划外）** —— S3 `GET /supplier/packing-tasks` 的可见性判据「本主体名下有启用中集散中心」在上述改动后**必然失效**，且其**数据面本身跨供应商**（打包线按场所聚合**他方**到货明细），开给任一供应商即**泄露同业经营数据**（违反 I1）；**只换判据治不了「不该看」** → S3 **整条路由迁运营后台** `GET /admin/packing-tasks`（P39「加工场所打包」），供应商端路由删除（→ `10004`），判据改为**不按供应商收窄**（`defaultPackingDate()` 取全量最近生产计划日）；⑤ **菜单与白名单同源**（`ADMIN_NAV` 加 `/supplier/packing-center`、`SUPPLIER_NAV` 删 `/supplier/packing`；`ADMIN_MENU_KEYS`/`SUPPLIER_MENU_KEYS` 同步）；⑥ **错误码零新增**、**表数仍 27**；⑦ e2e `§18`/`§20` 同步改写（D27 `50008`→`10004` · D30/D31 `50008`→`10001` · S3 段全量改打 admin 端点）；⑧ 纪律沉淀 → 《缺陷与陷阱》**#58**「**字段停用后，依附它的可见性判据会静默失效，且往往同时暴露一个合规漏洞**」 |
| v1.1.6 | 2026-09-17 | **M4-1 日切链路（4.1 / 4.2 / 4.3 + 调度基座 + 手动补跑接口 · 零 DDL · 零新增错误码）✅**：① ⭐⭐ **调度基座 `TASK_SCHEDULES` 单一真相** —— 8 个任务一张表写清「**几点触发** + **动哪一天**（`dateKind`: `today`/`tomorrow`/`yesterday`/`null`）」，任务体**永远不写日期计算**：日期由 `ScheduleService.run()` 从声明表推导并**回传给任务**，任务里根本没有写错日期的地方。⭐ 本批要防的头号陷阱是「**同刻不同日**」：`meal-publish` 与 `auto-confirm` 的 cron **完全相同**（`0 0 14 * * *`），但前者开「**次日**」的团、后者确认「**当日**」的单 —— cron 表达式只写「几点跑」、**不写「动哪一天」**，写反了照样编译通过、甚至部分断言仍是绿的。故 `GET /admin/schedule` **必须**下发 `dateKind`，这是该陷阱的唯一防线（e2e §28 直接断言两者 cron 相同而 `dateKind` 不同）；② **手动补跑接口** `GET /admin/schedule`（时刻表）+ `POST /admin/schedule/{task}/run`（`date` 可选，缺省按声明表推导）—— ⭐ **与跑批共用同一执行口**（任务的 `runOnce(date)` 就是其 cron 处理器内部调用的那一个方法），故「跑批算出来的数」与「补跑算出来的数」结构上不可能不同；⭐ **补跑刻意不加锁**：锁的语义是「同一目标日期只跑一次（防并发重入）」，而补跑的动机恰恰是「跑批没跑成」，被锁挡住即失去用途（重复补跑的安全性由**业务幂等**保证）；③ ⭐ **锁不是保险丝**：`KvService.setNx` 在 `memory` 驱动下**重启即丢**，把幂等托付给锁则一次重启就会重复执行跑批 → **成功保留锁、失败立即释放锁**（满足 4.11「失败可重放」）；④ ⭐⭐ **修掉一个真实数据缺口（备料量恒为 0 且不报错）**：`ab_meal_assignment.sold_count` 注释写着「实时累加」，但**全仓没有任何累加点**（只有种子赋过值），而它正是「推给供应商的备料量」的聚合基数 → 跑批推送的份数**恒为 0**。**修法不引入实时累加**（要在下单/取消/超时/退款四条路径同步维护，漏一条就是一次静默错账），改为**在截单这一「定格」时刻从订单表聚合一次并落库**，语义明确为「**截单定格的已售份数**」（截单前不承诺准确、截单后不再变 —— 自营口径下它是**采购款的基数**）；同时补 `SupplierService.freezeProducePlan()`：截单前若有人访问供应商工作台，会按**当时还在涨**的 `sold_count` 惰性生成并冻结一份**偏小的**计划，截单后必须**覆盖刷新**（只覆盖 `pending` 父行，已 `cooking`/`done` 的跳过并告警）；⑤ ⭐ **截单（4.2）三分支一次跑全**：`pending_pay → cancelled`（**同时解冻余额** —— 只改状态不解冻会让用户的钱永久锁在已取消订单上）· `paid → cut_off`（**不可逆**）· **定格备料量基数**；⭐ **顺序不可颠倒**（必须先截单再刷计划，否则计划量按未定格的销量算出来，而它一旦生成就冻结了）；⑥ ⭐⭐ **跑批路径也必须落 `ab_operation_log`**（状态机 §2.1.5）：全局拦截器只对 HTTP 请求生效，故两处迁移在事务内**显式** `OperationLog.insert()`（`snapshot.source='system'`）—— 否则「每次迁移留痕」在跑批这条路上是空的；⑦ **配送单（4.3）粒度 = 楼群**（唯一键 `(meal_date, building_group_id)`，与「一车货送一个楼群、团长在楼下收」一致），份数与备料量**同源**（否则会出现「供应商做 120 份、配送只送 100 份」而**两边都不报错**）；⭐ **幂等不覆盖**已存在行（司机/车牌是人工录入的，跑批抹掉就找不回来）；⭐ 新增**可信度告警**（当日无任何 `cut_off` 订单 → 明确提示「截单可能没跑成、份数可能偏小」，否则错到有人发现货不够）；`expected_at` 锚 **T 日 11:30**；⑧ ⭐ **未实装如实标注**：`auto-confirm`/`commission-settle`/`reconciliation` 出参 `implemented:false` + `pendingNote`（点明 M4-2），且**补跑被明确拒绝**（`10001`）—— 把「只打一行日志」的占位任务显示成「已上线」，比不显示更伤运营信任；⑨ ⭐⭐ **日期校验必须是 `isRealDate`**：新写的 `ScheduleAdminService.run()` 初版只做格式校验，而它的报错文案**已经**写着「且日历上真实存在」—— **文案承诺了一道校验、代码只做了另一道**；放行 `2026-02-30` 会让 `Date.UTC` **静默滚动**到 03-02，运营以为在补跑「2 月 30 日」并看到「完成」，实际把 **3 月 2 日**的订单批量置成了已截单（已在 e2e §28 钉死为 `10001`）；⑩ ⭐ **权限两级**：类级 `super_admin`/`admin`，**不含 `operator`**（补跑是**改写历史数据**的高危操作，与 P39 打包任务含 `operator` 的取舍**相反、理由也相反**），也不含 `viewer`/`finance`；菜单 `/system/schedule` 进 `ADMIN_NAV` 系统组 + `ADMIN_MENU_KEYS`（授权了就必须有入口）；S2 标 `@OperationLog`；⑪ ⏱ **e2e 时钟注入闸 `ABOX_SHIFT_TO_HOUR`**（`gate.mjs` 自动注入 `seed` / `e2e:m1/m2/m3`）：`isOrderable(T)` = `[T-1 14:00, T-1 23:00)` 使窗口外**任何**出餐日都下不了单，全量套件因此每天只有 9 小时能跑（**23:00–14:00 恒红**，m1 20 余条级联红、m2 更隐蔽地把 `undefined` 写进 SQLite 直接崩）。`time.ts` 收敛出唯一时间源 `now()`，注入时把**北京小时**平移、**日历日不变**、时间仍 1:1 前进（**只影响 `now()`，不改 `@Cron` 真实触发时刻** → 跑批验收一律走补跑接口）；配套给 m1/m2 加窗口前置**只给一条可读失败**；⑫ ⚠️ **顺手清掉三处「两个真相」**：`tasks.module.ts` 装配重写（补 3 个模块 + 控制器 + providers）、`delivery.controller.ts` 注释说明为何仍是空壳（**无后台修正入口，已挂账** #61）、`config.specs.ts` 内 5 个时间键的 `unwiredReason` 由「实际由 task 的 `@Cron` 硬编码」**改为与声明表一致**（原先指向不存在的占位任务，现在指向真实的 `TASK_SCHEDULES`）；⑬ e2e `§28` 新增 **30 条断言**（**不依赖下单窗口**：订单/分配行夹具**全部直插**，三个任务经**补跑接口 + 显式出餐日**驱动；四个隔离出餐日避开其它章节已用偏移；节末**彻底还原** —— 余额行与夹具用户不删会让平台负债凭空多出 ¥5.80 并让 D38↔D33 对账假绿）；同批次修掉两条**测试侧**教训（「别按字段名猜出参口径」——`plan.totalQuantity` 是**各菜合计**而非份数，改为与落库 `SUM` 交叉核对）；⑭ **门禁 `all` 15/15 + `verify` 4/4 全绿**（m1 38 · m2 121 · **m3 808**，较上批 +30）；纪律沉淀 → 《缺陷与陷阱》**#59 / #60 / #61** + §三 三条 |

| v1.1.7 | 2026-09-17 | **M4-2 结算链路（4.4 自动确认 / 4.5 佣金入账 / 4.7 对账 · 零 DDL · 零新增错误码）✅**：① ⭐⭐ **佣金改「两段式」**（本批头号决策）—— 旧实现「L9 取餐确认即入账」（`accrueForOrders` 直接写 `settled` 并加余额）把「计佣」与「入账」合成一件事，于是**退款落在两者之间的窗口时无路可走**。改为 `T 日 14:00 计佣 → ab_commission(status='pending')`（**只记账、钱不动**）+ `T+1 02:00 入账 → settled + ab_balance`（`creditCommissions` 是**唯一**把钱写进余额的地方，只被 `settlePending` 调用）。故 `pending` 成为**每天都出现的正常中间态**，`scanned=0` 的含义收窄为「当天没有新确认的订单」（**记 log 不记 warn** —— 否则运营每天凌晨被一条假告警吵醒）；② ⭐⭐ **退款冲销拆两条路径（C9 修订）** —— ① 退款落在**未入账窗口**（`pending`）→ **只把原行作废**（`pending → cancelled`）、**不写负向冲销行、不动余额**，`notes` 明写「未动余额」；② 佣金**已入账**（`settled`）→ 写 `type='reversal'` 负行 **+ 扣余额**（发生额历史保真）。⚠️ **旧实现只有路径②** → 在 ① 的窗口下会**从余额里扣一笔从未入账的钱**，而 `ab_balance` **允许为负**（佣金已提现即形成欠款，是**刻意**设计）→ **扣成负数、形成假欠款，且不报任何错**（《缺陷与陷阱》#62）；⭐ 出参符号与「是否已入账」**无关**（`commissionReversedFen` 恒正 / `commissionReversedQuantity` 恒负），端上不必分叉；③ ⭐⭐ **4.4 只转 `delivered`** —— `pending_pay`/`paid`/`cut_off`/`cooked`/`delivering`/`refund_applying`/`refunding` **一律原样不动**，收进 `notDelivered.byStatus` + `logger.warn`（到 14:00 还没送达是**履约异常**，把它当「确认收货」会把异常洗成正常单、佣金还照计 → **fail-closed**）；⚠️ 与 **L9 手动确认刻意宽严不同**（L9 放行 `delivered`/`delivering`：团长在楼下收款时可能面对刚到货的实况），差异已登记**待裁决**；⭐ **无归属团长**仍转 `completed` 但**收口计佣为 0**（`orphanConfirmed` + 日志，不猜、不建号）；④ ⭐⭐ **4.5 与 D35 同一执行口**（`CommissionSettleTask.runOnce()` 就是 `CommissionService.settlePending()`）→ 「跑批入账的数」与「运营点按钮入账的数」结构上不可能不同；⚠️ **不做「补计佣」**（`ab_order` 无「确认时刻的团长等级」快照 → 事后只能读**当前**值 → 晋级后必然多算且无法自证，#51）；**补账只改账不改事实**（不动 `total_orders`/`last_order_at`，否则污染 C2 晋级审计）；⑤ ⭐⭐ **修掉 `monthOrdersOf` 的双计** —— 旧写法 `SUM(CASE WHEN type='reversal' THEN -quantity ELSE quantity END)` 配 `status` 过滤，会让退款单**先被 `WHERE` 排除、又被 `CASE` 加回**（`reversal` 行 `quantity` 本为负、`CASE` 又翻正）→ 月单**不降反平**，**可凭退款刷 C2 晋级**（退款是免费的）；现只数 `type='normal' AND status IN ('pending','settled')`（#63）；⑥ ⭐⭐ **4.7 目标日期 `today` → `yesterday`（改正）** —— 每日 04:00 对「今日」只能核 `00:00–04:00` 这 **4 小时切片**，昨日 23:00 之后（下单窗口 `[T-1 14:00, T-1 23:00)` 的尾部）的流水要等**次日**才被覆盖 → **每天都漏核一段**，而每天的结论都「看起来是对的」（#64）；⭐ **锚 = 支付日**（`paid_at`，财务域**唯一**不指出餐日的端点）；⑦ ⭐⭐ **对账不平的告警落操作日志**（用户 2026-09-17 裁定）—— 落 `ab_operation_log`（`module='finance'`/`action='对账不平'`/`target_id=日期`），后台「操作日志」页直接可见、可人工跟进（**零 DDL**，不建对账异常表）；**平了不写**（否则一年 365 条噪音会把页面淹掉）+ **幂等**（同日已有告警不重复写，补跑只回报 `alerted=false` + `alertSkippedReason`）；⭐⭐ 告警快照**自带 caveat**「仅本地三头核对，**不等于已与微信侧对平**（一期无商户号与账单文件）」+ `channel.source='local_only'` + `billAvailable=false` —— 「不能把告警藏在服务器日志里」，也不能让它看起来比实际更可靠（同 #55）；⑧ ⭐⭐ **8/8 全部实装** —— `GET /admin/schedule` 的 `summary.implemented` 由 5 升到 **8**，§28 的三条「未实装 → 补跑被拒」断言**翻转为「已实装 → 补跑返回真实结果」**；`PENDING_NOTE` 表**刻意保留为空表**（`implemented` 是 `runners.has(task)` 的**派生值**而非手写常量 —— 将来只加声明不写执行口，页面会自动如实显示「未实装」）；⑨ ⭐ **激活一条从未运行过的守卫** —— `collectQuitBlockers` 的「有未结算佣金不得停职」在两段式前**恒不触发**（`pending` 永远为 0 → **死代码**），两段式后 `pending` 天天产生，守卫**真正开始运行**（#52 已由「登记待观察」转为「已激活」，e2e 直插 `pending` 钉死）；⑩ **零 DDL、零新增错误码**（仅复用 `10001`）；e2e **§29 新增 10 条不变量**（三个隔离日期 `today−208/−209/−210`，节末含 `ab_refund` 全量还原）+ **§28 三条断言翻转**；⭐ 同批次修掉一条**只在串跑下红**的断言写法（余额「未动」应写成**前后相等**、不能写成「等于 0」—— 单跑时该用户连余额行都还没建、串跑时 m1/m2 已留下非零余额，写 0 只会在串跑下红且**与被测行为无关**，同 #48 教训） |
| v1.1.8 | 2026-09-17 | **M4-3 队列消费者 + 订阅消息投递点（4.9 / 4.10 · 零 DDL · 零新增错误码）✅ —— M4 全部收口**：① ⭐⭐ **外部支付通道调用移出 DB 事务（本批头号改造）** —— `RefundService.executeRefund` 原在**事务内**先调微信退款，失败即抛 `40010` 让整笔回滚。这看似避免了「有退款单没退款」，实则留下**更严重且不可自愈**的反面：**微信退款成功之后，事务若因任何原因失败（冲销报错 / 连接断），钱已经出去了，而系统里没有退款单、订单也没变** —— 账实不符，且**没有任何机制能发现它**。改为两段：**事务 A**（纯 DB：冲销 + 退款单 `refunding` + 订单 `refunded`）**先提交** → **事务外**调通道 → 成功走**事务 B** 收口 `refunded` + `wx_refund_no`；失败则退款单 `failed` + **入队 `refund-apply` 退避重试**（`refundNo` 是微信侧**幂等键** `out_refund_no` → 重试**不会重复出款**），重试耗尽进死信并落 `module=queue` 操作日志。⭐ 对外语义随之收窄为「**已受理，通道那段待重试**」：订单**仍 `refunded`**、账务**不回滚**（回滚会造成「余额退回来了又扣走」这种用户可见的反复）；② ⭐ **三消费者实装**（`order-paid` / `refund-apply` / `settle-orders`）—— 消费者是**薄适配层**（只注册 + 转发），业务逻辑仍在原服务里（不复制第二份）；`GET /admin/queue` **如实报告「会不会丢任务」**（memory → `durable=false`，**不做静默降级** —— `KvService` 那种「连不上就退回内存 + WARN」在队列上不成立：丢一条「退款待重试」**既无报错也无处可查**）；`failed` 口径 = **重试耗尽进死信**（不是「失败过一次」—— 中间重试还在 waiting/delayed）；③ ⭐⭐ **`settle-orders` 载荷由「整批一条」改为「按团长逐条」** —— 订阅消息**必须能寻址到具体收件人**，聚合载荷（`mealDate/settledCount/totalFen`）里没有收件人，消费者只能写日志：**「通知用户」实际没发生却看起来成功**；拆成一人一条后独立重试、**死信粒度到人**；④ ⭐ **新增两个真实投递点**：`leader_apply`（团长申请确认，`team-leader.service.apply` **事务外**通知）+ `commission_settled`（佣金入账，`settlePending` 事务提交后**逐团长入队**）—— 通知一律**在事务外 + 服务内 try 兜底 + `notify()` 不抛异常**（**双保险**），确保通知永远无法把一笔成功的业务变成失败（e2e §30：通知失败仍返回 `isLeader=true`）；⑤ ⭐ **场景 5 行 → 6 行**（新增 `commission_settled`），每个场景补 `requestSubscribe`（**机器可读布尔** —— 不能从 `audience` 中文反推，否则「改文案即静默失效」）+ 出参 `subscribeTemplateId`（**此刻实际可授权的模板 ID**，`null` = 不该请求）；⑥ ⭐ 新增 **U18 `GET /me/subscribe/templates`**（用户端订阅授权清单，**四条件过滤**：用户端场景 ∧ 已接线 ∧ 已启用 ∧ 已配模板 ID），并下发 `note`（「列表为空**通常是正常的**」+ 微信订阅消息是**一次性授权**、没授权服务端推不出去会回 `43101`）——「必推项」指的是**产品意图**，不是「无需用户同意」；⚠️ 原型 **U15 `POST /me/subscribe`（上报授权结果）一期未实装**（需新表 + 无微信账号无法验证），**明确留名、不假装**；⑦ ⭐ **接线口径两级如实标注**：`wiring=live`（有真实投递点）与 `enabled=1`（配置就绪）是**两件事** —— 一期 3 个 live 场景**全部 `enabled=0`**（缺微信模板 ID），页面据此解释「为什么后台能看到场景、用户却收不到」（同 M3-10「给了输入框却没接上线」纪律）；⑧ ⚠️ **顺手修掉一个「四处都不报错」的静默失效**：`refund_result` 的通知落地页原硬编码 `pages/order/detail`，而 `pages.json` 的真实路由是 `pages/order-detail/order-detail` —— 投递日志显示成功、微信也照发，用户点开却落到**不存在的页面**（**微信不校验 `page`、服务端也不解析小程序路由**）；改用 `NOTIFY_PAGES` 单一常量，并在 e2e §30 补一条 **`NOTIFY_PAGES` ⊆ `pages.json` 的机械对账** —— ⭐ **把已登记的风险变成强制断言**（#67）；⑨ ⚠️ **修掉一个「编译期全绿却起不来」的运行时缺陷**：`RefundService` 新增注入 `OperationLogRepository` 后服务启动即崩（`Nest can't resolve dependencies ... at index [7]`），根因是 `FinanceModule` 的 `forFeature` 未注册 `OperationLog` ——⭐ **`@Global()` 只让该模块 `exports` 的东西全局可见**：`CommonModule` 虽 `@Global()` 且已 `forFeature([TeamLeader, OperationLog])`，但 `exports` 里**没有** `TypeOrmModule`，从别处 import 进来的 provider **不会二次导出**，故那两个仓储只在它**自己的上下文**可用（谁注入谁注册，与 `tasks.module.ts` / `admin.module.ts` 既有做法一致）；⚠️ **`tsc` / `eslint` / `build` 全都查不出 DI 错误**（依赖解析发生在**运行时**）→ 纪律：**每批改动后必须真起一次服务**，否则「门禁全绿」只是关于**编译**的（#66）；⑩ e2e **§30 新增 68 条断言**（**不依赖下单窗口**）：A 队列状态端点（`driver=memory`/`durable=false`/三队列在册/`attempts=3`/白名单含 `operator` **不含** `finance`·`viewer`）· C `settle-orders` **真跑批**（`notifyQueued=1` / 消费 `completed≥1` / 未启用**不投递且不留日志** / 幂等重跑 `notifyQueued=0`）· D `refund-apply` **通道失败注入**（→ `wxDelivered=false` / 单据 `failed` / **不写 `wx_refund_no`** / 重试耗尽进死信 + 操作日志可人工处理）· E 成功对照（`wxDelivered=true` / `wx_refund_no` 落库）· F `leader_apply` 投递点（通知失败不影响 `isLeader=true`）· G 订阅授权清单（一期空 + `note` 口径 + 配好模板 ID 后恰好出现 1 条 + **四条件过滤真的在过滤** + 结构性不变式 `requestSubscribe=true ⇒ wiring=live`）· **G2 跨端落地页机械对账** · H 夹具**全量还原**（含死信日志；**不删模板行**只还原取值）；§24 断言由「5 个场景」改「6 个场景」；⑪ **门禁 `all verify` 15/15 全绿**（m1 38 · m2 121 · **m3 808 → 876**，较上批 +68）。
| v1.1.9 | 2026-09-17 | **M4-4 提现审批 D45 + D46/D46a/D46b/D46c 落地（P34 · 模块 M35-08 · 零 DDL · 新增 `40016`/`40017`）✅ —— 收口 L12 打开的资金链**：① ⭐⭐ **本页不是报表，是「钱出不去」的解法** —— L12（团长提现申请）从 M2 起就实装（申请一瞬间 `balance −X` / `frozen +X`），而此前后台**没有任何端点能把这张单往前推**：① 团长的钱被**永久锁死**（`frozen` 只增不减，没有任何界面能让它下降）；② `collectQuitBlockers` 以 `pending`/`approved`/`paying` 判定「有未完成的提现」→ 提过一次现后 C3 退出团长**再也不可能成功**，而错误文案还写着「等待提现到账」（等一个永远不会发生的到账）；③ `ab_team_leader.withdrawn_amount` 永远是种子值（无写点）。故本批定位 = **把 L12 打开的资金链收口**；② ⭐⭐ **三路解冻共用一个 `releaseFrozen()`**（驳回 / 到账 / 失败），差别只在 `direction`：`'in'`（驳回/失败）= 把钱挪回可用（`balance +X` / `frozen −X`，**`total_in`/`total_out` 都不动** —— 钱没进出平台，只是从「冻结」挪回「可用」，同 D39 口径）；`'out'`（到账）= 钱正式出平台（`frozen −X` / `total_out +X`，可用余额不变）。⚠️ **三路各写一份的后果不是重复代码，而是其中一路漏掉某个字段**（如「到账忘了减 `frozen`」→ 该用户冻结额永久虚高，而同一天其余提现看起来都正常）；③ ⭐⭐ **`total_out` 按「申请金额」而非「实付」累加** —— 申请时已扣走 X，到账只是把这笔被冻结的钱正式记为支出；按实付记会让 `total_in − total_out` 与 `balance + frozen` 之间**永久**留下一个等于代扣税额的缺口（账面像「平台多留了钱」，而那笔税是平台**代扣代缴给税务**的、不是平台留存）—— e2e 用「到账前后 `(total_in − total_out)` 的差值 === 申请金额」钉死；④ ⭐⭐ **到账不写 `ab_balance_log`** —— 该表语义是「**可用余额**的每一次变化」（`balanceAfter` 逐条落痕、末条 === 当前余额，L11 ↔ L19 靠它互验），到账时可用余额**不变**（钱在申请时就已扣走）；这不是「账本与快照不同源」：`frozen` / `total_out` 的变化**从来**不由流水解释（D39 的 `freeze` 行即先例），到账事件由 `ab_withdraw` 自身完整承载 —— e2e 把「到账后流水条数不变」写成断言，让「为什么没有」成为**声明**；⑤ ⭐⭐ **解冻前 fail-closed `40015`**（`frozen < 申请额`，**不复用 `40002`**）：前者是「**冻结账对不上**」的账实不符信号（有人绕过了冻结口径）要查数据，后者是「钱不够花」；硬扣会让 `frozen` 变负，并在**下一个用户**那里表现为「冻结额凭空多了」；⑥ ⭐ **状态机守卫 `40017`**（**不复用 `40013`** —— 形态相同但**排查入口不同**：退款单去 D40 看、提现单去 D45 看），四动作两两之间都可能被误操作且每次后果都是**再动一次钱**，故一律回「当前状态「已到账」不支持批准」这种能直接照着排查的话（`data.allowed` 回带允许集合）；**未知单号 `40016`** 与 `10004` 分开（后者含「在库但无权限」）；⑦ ⭐ **到账登记自洽校验** `申请额 − 代扣 = 实付`：两栏都传却不自洽 → `10001`（不允许两处各记一套 —— 那必然产生「账上代扣 50、实付另算」的双真相）；两栏都不传 → 视为无代扣但出参标 `taxSource='assumed_zero'`（**让「系统替你假设了 0」看得见**，否则「实付 = 申请额」看起来像一个已核实的结论）；⑧ ⭐ **`payoutBatchNo` = 审批日批次** `PB{yyyyMMdd}`（**刻意不含随机位**）：单号要「一单一号」（唯一）、批次号要「**同日同批**」（聚合 —— 一期人工通道下运营每天集中汇总一份清单提交平台、回执也整批回来）；需一天内分批时由操作者显式覆盖，**不自造「当日第 N 批」计数器**（跨请求读-改-写并发会撞号，而批次号**不是**资金主键）；⑨ ⭐ **汇总两类量刻意分开**：在途量（`pending*`/`approved*`/`paying*`/`frozenByWithdrawFen`）取**全量、不随筛选变化**（「平台此刻因提现占用了用户多少钱」不该因搜了个姓名就变小）并带 `asOf`，⭐ `frozenByWithdrawFen` == 三状态金额之和、可与 `ab_balance.frozen` 的**增量**互相验算（后者还含 D39 手工冻结）；历史量（`paid*`/`released*`）取**同一过滤条件的全量**（不受分页影响）；⑩ ⭐⭐ **财务域角色白名单收敛为单一真相** —— `finance.constants.ts` 的 `FINANCE_READ_ROLES`（类级含 `operator`）/ `FUND_ACTION_ROLES`（方法级资金动作，**不含 `operator`**）由 **D35 / D39 / D41 / D42 / D46 系列共用一份**（此前五处各写字面量，副本的后果**不是编译失败而是静默漂移**：「按钮亮着、点了 `10003`」或更糟的「按钮灰着、其实有权限」）；出参 `actions.canAudit` 与 `@Roles(...)` 共用同一常量；⑪ ⭐⭐⭐ **顺手修掉一条用户直接看得见的错（真缺陷 #69）**：`ab_team_leader.balance` / `withdrawn_amount` / `pending_amount` 三列**只在种子里被赋过值、全仓没有任何写点**，却被登录（A1）、团长资料（L14）、退出团长（L20）三处出参当作「可用余额 / 累计已提现 / 待入账佣金」下发 → 团长「我的」页显示**种子里写死的数字**（示例 ¥575.86），而「余额明细」页（L11 走 `ab_balance`）显示真值 —— **同一个人在同一时刻看到两个不同的余额，两边都不报错**；去提现又被 `50004 可提现余额不足` 挡下，而页面上明明写着有几百块（**必然产生的客服工单**）；修法**不是把三列写起来**（那就是第二份真相，漏掉任一写点就会悄悄分叉）而是**不再读** —— 新增 `LeaderMoneyService`（`common/services/`，四值全部**派生**：余额/冻结 ← `ab_balance`，待入账 ← `ab_commission(status='pending' ∧ type='normal')`（⭐ 必须排除 `reversal` 负行），累计已提现 ← `ab_withdraw(status='success').actual_amount`（**到账口径** —— 代扣的个税没到团长手里）），登录/资料/退出/后台团长列表与详情**五处共用同一份口径**，三列转**历史字段**（列保留 · 不再读写 · 与 M4-0 同一处理）；⭐ 同批修掉 `getBalance()` 的两处静默错算：`pendingCommissionFen` **漏排 `type='reversal'`**、`inFlightCount` **只数 `pending` 而注释写着「待审批 / 已批准 / 打款中」**（文案承诺三个状态、代码只数一个 → 运营一批准该数字立刻掉到 0，团长以为「没有处理中的提现」而那笔钱仍在冻结中）—— 改用 `WITHDRAW_FROZEN_STATUS`（shared-types 单一真相），**不手写状态列表**；⑫ ⚠️ **`PayoutChannel` 枚举值大小写漂移（真缺陷 #70）**：枚举原写小写（`'flex_manual'`），而实际链路上**全是大写**（列默认值 / `BizConfigService.payoutChannel()` 缺省值 / e2e-m2 断言全是 `'FLEX_MANUAL'`）—— 此前因该枚举**全仓零消费点**才没炸；一旦有人照它写 `row.payoutChannel === PayoutChannel.FLEX_MANUAL`，比较会**恒为 false 且不报错**，故改为与落库值逐字一致；⑬ **零 DDL**（`ab_withdraw.payout_batch_no` 列早已存在）· **表数仍 27** · 新增错误码 `40016` / `40017`（已回写《接口规范》§九 + 扩展码登记）；⑭ **菜单与白名单同源**：`/finance/withdrawal` 进 `ADMIN_MENU_KEYS` + finance 角色菜单 + `ADMIN_NAV`（**授权了就必须有入口** —— M3-14 曾漏挂 5 个子页，运营只能手输 URL）；⑮ 前端 `withdrawal.vue`（四 Tab + 在途/本筛选两组汇总 + 权限提示 + 四弹窗；到账弹窗「代扣/实付」用 `number \| null \| undefined` 三态 —— `el-input-number` 清空 emit `undefined`，只判 `=== null` 会漏；`paidHint` 与后端**同一套推导规则**）；⑯ e2e **§31 · 49 条断言**（**不依赖下单窗口**：**专用团长夹具**（不借既有团长 —— 避免别人的钱造成假绿）+ D39 充值建户 + 四笔申请走 `success`/`rejected`/`failed`/`success`，覆盖**状态机 fail-closed** · **驳回/失败精确复原**（`balance +X` / `frozen −X`，两累计不动）· **到账按申请额计支出** · **到账不写流水** · **代扣/实付自洽** · **会计恒等式 `total_in − total_out === balance + frozen`** · **两级白名单**（读含 `operator` 不含 `viewer`、写不含 `operator`、未登录 `10002`、未知单号 `40016`）· **在途提现挡住退出团长**（`20008` + `WITHDRAW_IN_FLIGHT`）与「全部到终态后该腿闭合」；节末**全量还原**（提现单/余额/流水/团长/用户/操作日志）→ `frozenByWithdrawFen` 基线 Δ=0）；⑰ `SCAFFOLD_KEY` 增 **4 项** M4-4 契约载体；⑱ `gate.mjs all verify` **15/15 全绿**（m1 38 · m2 126 · **m3 876 → 925**，较上批 +49）。 |
| v1.1.10 | 2026-09-17 | **M5-0 部署运维基座落地（M5 前置子批次 · 本地已完成 · 静态验收 · 零 DDL · 零错误码 · 表数仍 27）🔶 —— M5 主线被外部条件（云资源 / 域名 / 真实账号）阻塞，故先把「不依赖外部条件的那部分」做完**：① ⭐⭐ **盘出并修掉 5 条「从未被执行过」的真缺陷（#71–#75）** —— 这一批的共同特征是**所有机械证据都是绿的，而对应的能力从未生效过一次**：**#71** `main.ts` **从未调用 `app.enableShutdownHooks()`**（Nest 默认不注册信号监听）→ 项目里两处 `onModuleDestroy()` **一次都没跑过**：容器 `docker stop` 后**不等在途请求跑完**就杀（正撞「支付回调处理到一半」），队列消费者不 `close()` → BullMQ active 任务被丢在 Redis 上等 `stalledInterval` 才被重新认领（**不报任何错**）；长期没暴露是因为 e2e 收尾走 `taskkill /T /F` **强杀**、**从没走过优雅关闭这条路**，而本地 Ctrl+C 看起来「也退出了」；**#72** `data-source.ts` 的迁移 glob 只匹配 `*.ts`，生产跑 `dist/*.js` → **命中 0 个文件**，而 TypeORM 对「没找到迁移」**不报错**、只打印 `No migrations are found` 然后 `migration:run` **正常退出 0** → 部署流水线里「迁移」这一步显示 **✅ 成功而 DDL 从未应用**（症状会伪装成「代码把列名写错了」）；**#73** `scripts/deploy.sh` 是**空壳**（引用不存在的 Dockerfile + 占位 registry + `kubectl apply`），**从未被执行过一次**，却让人相信「部署脚本已经有了」；**#74** 骨架 `Dockerfile` 五处生产硬伤（`--frozen-lockfile=false` 构建不可复现 / 缺 `build:shared` / root 运行 / 无 tzdata 与 `TZ` / 无 `HEALTHCHECK` 且 **shell 形式 CMD 吞 SIGTERM**）+ compose 传了 `APP_VERSION` 而 Dockerfile **没有 `ARG` 声明** → 版本注入静默失效；**#75** `/health` 只证「进程会应答」却被当「可以接流量」用 —— 若 liveness 也检 DB 则 DB 一抖即**重启风暴**（DB 挂了重启应用根本治不好）、若只保留不检依赖的版本则**摘不掉流量**；② ⭐⭐ **探针分级（#75 修法）**：`GET /health` 出参**一字不改**（三套件 `waitHealthy()` 依赖 `HTTP 200 && code===0`）+ 新增 `GET /health/ready` 检 DB `SELECT 1` 与**队列 3 个消费者是否齐备**，失败时**显式** `BizException(SERVICE_UNAVAILABLE=90002, …, HttpStatus.SERVICE_UNAVAILABLE, body)`；⚠️ **必须显式传 503** —— `90002` **不在** `HTTP_STATUS_OVERRIDE` 表里，而本项目业务失败默认 HTTP 200，不传就退化成 200，**编排器只看状态码 → 「不可用」被判成健康**；⭐ readiness 检「消费者齐不齐」而**不是**「Redis 通不通」—— 连通性在**启动期已 fail-closed 验过**（连不上即拒绝启动），而「消费者没注册上」是**启动期验不到、失败后完全静默**的一类故障（入队照常成功、`GET /admin/queue` 计数照常显示、任务**永远无人消费**，同 #68）；⭐ 探针**免鉴权 = 公网可见**，故出参按「会被人看见」设计（失败原因只进服务端日志，HTTP 给固定文案「数据库探测失败（原因见服务端日志）」）+ `PROBE_TIMEOUT_MS=1.5s`（依赖「半死」时不挂住）；③ ⭐ **优雅关闭（#71 修法）**：`enableShutdownHooks()` + 容器 **exec 形式 CMD**；⚠️ **验收方式本身有坑** —— Windows 上 `process.kill(pid,'SIGTERM')` 走 `TerminateProcess`、handler **不跑**，故改为进程内 `process.emit('SIGTERM')` 等价触发，并用**跨进程子脚本**在钩子执行瞬间 `writeSync(1,…)` 落标记、父进程读（**「进程退出了」不算证据，强杀也会退出**）；④ ⭐ **生产迁移（#72 修法）**：glob 改 `*.{ts,js}` 双匹配 + `db:migrate:dist` / `:revert` 指向 `dist/database/data-source.js`；**验证** = `typeorm migration:show -d dist/…` 必须**列出** `[ ] Init1700000000000`（证明 glob 真的命中了编译产物）；⑤ ⭐⭐ **镜像与编排（#73/#74 修法）**：`apps/api-server/Dockerfile` 多阶段重写（builder `--frozen-lockfile` → `build:shared` → `api build`；runner `node:22-alpine` + tzdata + `ARG APP_VERSION` → `ENV` + **非 root** + `HEALTHCHECK` 打 `/health` + exec CMD）+ 新增 `apps/admin-web/Dockerfile` 与 `nginx.conf`（SPA fallback / `index.html` no-store / assets immutable / `/api` 与 `/static` 反代 / `proxy_request_buffering off` / gzip / 安全头）+ `docker-compose.prod.yml`（四服务 · mysql/redis **不暴露宿主端口** · `${MYSQL_ROOT_PASSWORD:?}` **fail-fast** · `init: true` 配套 shutdown hooks · 资源限制 · 日志 `max-size:50m`）+ `.dockerignore`；⑥ ⭐ **运维脚本 5 件**：`deploy.sh` 重写为 7 步（`预检 → 备份 → 起依赖 → 构建 → 迁移 → up → 健康验证`），**失败时不自动回滚**（半新半旧比全旧更危险）改为**停 + 打印回滚命令**；`rollback.sh`（默认**不退迁移**，`--with-migrate-revert` 才退；回滚前自动备份）· `backup-db.sh`（`--single-transaction` + **size 检查防空导出**）· `restore-db.sh`（**破坏性 → 必须 `--yes`**）· `healthcheck.sh`（退出码 0/1/2 **可直接进 CI/cron**）；⑦ ⭐ **《ABox一盒部署运维手册 v1.0》**（环境矩阵 · 拓扑 · 密钥纪律 · 部署流程含首次核对 9 项 · 禁发窗口 · 两级探针监控 + 8 任务时刻表 + 队列观测 + 告警 · 备份恢复 · **应急预案 6 类** · 日巡检清单 · **遗留风险 9 条**）；⑧ ⭐⭐ **回归保护**：探针契约断言**并入 `e2e-m1.mjs`**（7 条：liveness 契约 · 出参键集合未被就绪信息污染 · readiness 检 DB · 3 队列消费者齐备 · 版本与 uptime 回显 · **免鉴权探针不泄露主机 / 端口 / 连接串** · 对照「受保护端点无 token 仍 401」）—— 理由：探针是**编排器的判据**，改坏了**不会有任何功能报错**，只会让「该摘流量时摘不掉、该重启时重启不了」，故必须钉进门禁；**实测 m1 38 → 45**；⑨ ⚠️ **遗留风险如实标注（不假装已验）**：本机**无 Docker** → 镜像与脚本**只做静态审查**（52 项机械检查 + `bash -n`），**未在真实服务器上跑过**；生产库**从未执行过迁移** · e2e 用 sqlite 非 MySQL · 微信回调与 HTTPS 未在真实通道验过 · **未压测** · 镜像未瘦身 · 账号资源未到位 · 食品经营许可证仍待办（**九条逐项列在手册 §遗留风险**）；⑩ 新增缺陷登记 **#71–#75**（《缺陷与陷阱》）· `SCAFFOLD_KEY` 增 **15 项** M5-0 契约载体 · `gate.mjs all verify` **15/15 全绿** · e2e **m1 45 / m2 126 / m3 925**。 |
| v1.1.11 | 2026-09-17 | **M5-1 配送单人工修正落地（M5 前置子批次 · 收口挂账 #61 · 零 DDL · 新增 `30016`/`30017` · 表数仍 27）🔶**：① ⭐⭐ **口子从 DBA 手里收回运营手里** —— 4.3 生成配送单后，`total_quantity` / 司机 / 车牌 / 备注此前**没有任何后台入口**，只能进库改；而 4.3 的幂等**刻意不覆盖**已存在行（保护人工录入的司机/车牌），于是「份数要改」也无路可走 → 本批实装 **D61 `GET /admin/deliveries`** + **D62 `PUT /admin/deliveries/{id}`**；② ⭐⭐ **乐观锁 `version`（新码 `30016` + `data.current`）** —— `ab_delivery_record.version` 列**早就存在、此前零使用**；不加锁的两个运营同时改同一张单，就是**后写静默覆盖前写**：改份数的人改完，司机被另一个人换成另一个名字，**两边都显示成功**；冲突时回带当前值让前端「刷新后重提」，而不是让人对着旧值反复撞；目标 id 非法 → **`30017`**（与「在库但无权限」`10004` 分开）；③ ⭐⭐ **份数不符 ≠ 被人改过** —— 列表带 `orderQuantity`（截单实收份数）与 `quantityDiff`，但**两种成因刻意不区分**：截单没跑成（份数偏小）与人工修正，在数据上长得一样。故下 `quantityMismatch` **有歧义**的标记同时，另下 `hasManualInput`（是否有人工痕迹）**可靠**标记，`note` 直写这层歧义 —— 不给歧义，运营就会把「跑批没跑成」当成「有人偷改」；④ ⭐⭐ **审计 diff 不新建表** —— `OperationLogInterceptor` 的 `snapshot` 列只存 `{operator, role, at}`，但 `response_data` **整体落库**；故 D62 出参带 `{before, after, changed}` 即被自动留存（**成功与失败都记**，e2e 分别钉死）；⑤ ⭐ **份数聚合单一口径** `aggregateOrderQuantity()` 由 4.3 跑批与 D61 列表**共用**，且对比**取全量**（不受 `buildingGroupId`/`status` 筛选影响 —— 筛完再比会让「一筛就全部对不上」）；⑥ ⭐ **枚举收敛** —— `DELIVERY_STATUS_TEXT` 原在两处逐字重复（`leader-order.service.ts` / `workbench.service.ts`），本批收敛到新增 `shared-types/src/enums/delivery-status.ts`（新建的后台页若各写一份，就是**第三份**，同 #67）；⑦ ⭐ **只改 5 个字段、不含 `status`** —— 状态（发货/在途/到达）属**履约流转**，与「修数据」的风险不同（改状态会影响下游对「是否已送达」的判断），留独立批次；**无 POST / DELETE**（配送单由跑批生成，不做手工增删）；⑧ **两级权限白名单**：类级含 `operator`（要跟单），**不含 `viewer`**（配送单含运力与司机电话）、不含 `finance`（与 D47–D50 看板刻意含 `viewer` 正好相反）；⑨ ⚠️ **#49 本批只做文案纠偏**（不接线）—— 5 个时间键的 `unwiredReason` 由「实际硬编码在 `xxx.task` 的 `@Cron("…")`」改为指向 `TASK_SCHEDULES` 调度声明表（M4-1 之后原文案已**过时**），`set_meal.cutoff_time` 额外写明「本键/声明表/下单窗口三者**无机械对账**」；**接线调度会动下单窗口**（e2e 全量依赖 14:00–23:00）→ 仍为**独立验收项**；⑩ e2e **§32 新增 25 条断言**（隔离日 −216/−217 · 节末全量还原）+ §28「人工录入」由**直写 SQL 改为走 D62 接口**（顺带让「修正真的落到库里」成为断言）+ §22 断言文案随 ⑨ 更新 → **m3 925 → 951**；⑪ `gate.mjs all verify` **15/15 全绿** · `SCAFFOLD_KEY` 增 **6 项** M5-1 契约载体。 |
| v1.1.12 | 2026-09-17 | **M5-2 生产迁移补齐 + 结构对账门禁（M5 前置子批次 · 收口 #76 · 增量迁移 ×1 · 零错误码 · 表数仍 27）🔶**：① ⭐⭐ **全仓唯一那支迁移比实体少 2 表 / 9 列 / 1 索引，且从未在任何环境执行过** —— `data-source.ts` 里 `synchronize: driver === 'sqlite'`，**本地全部验证（seed / e2e×3 / gate）都走 sqlite 由实体同步建表**，于是「实体改了、迁移忘了改」**不在任何失败路径上**：e2e 全绿，而生产首迁建出的是**残缺库**。⚠️ **危险度比 M5-0 之前更大**：#72 刚把 glob 从 `*.ts` 修成 `*.{ts,js}` —— 形态从「**静默零迁移**」升级为「**跑出一个残缺库**」。⚠️ 缺列**不是「少个功能」，是「整张表打不开」**：TypeORM `find()` 默认 SELECT 实体全部列 → MySQL 上 `ER_BAD_FIELD_ERROR`，后果是后台**供应商列表/详情**（D23/P25）与**楼栋列表**（D37）**打开即 500**、**退款审批（C6）**读写 `ab_refund` 全表打不开（fail-closed 的 `40014` **连触发机会都没有**）；② ⭐⭐ **修法选「新增增量迁移」而非「就地改 init」** —— init 从未被执行过、就地改更干净，但**若某环境已把 `Init1700000000000` 记为已执行**，TypeORM 会**静默跳过**改动后的它（正是本条最恨的失败形态）→ 增量迁移**不再依赖「没人跑过」这个前提**；代价是多一支文件；③ ⭐⭐ **缺口精确化靠机械对账、不靠人读** —— 新增 `schema-parity.ts`：源A「**迁移推演**」= 解析全部迁移的 `CREATE TABLE` / `ADD COLUMN` / `MODIFY COLUMN`（作用域 = 模块级常量 + `up()`，`down()` 排除，**先剥注释再扫关键词**）；源B「**实体真库**」= TypeORM 拿 `ALL_ENTITIES` 在**内存 sqlite** 上 `synchronize` 真建表再读回。**源B 刻意不解析实体源码**：列装饰器形态太多（`@Column`/`@VersionColumn`/`@PkColumn`/`@CreateDateColumn`），正则解析必漏 —— **本批前两版手写解析器就是这么错的**（漏 `@VersionColumn` / 行号错位），故改用「让 ORM 自己建一遍」；④ ⭐ **三处刻意豁免 / 显式失败**：**PK 列豁免类型比较**（sqlite `integer` vs mysql `bigint` 是 `PkColumn()` 按 `DB_DRIVER` 的**刻意分派**，不是漂移）· `JSON→TEXT` 归一 · **未登记类型 / 未支持语句形态**（`CHANGE COLUMN` / `RENAME` / `CREATE INDEX` / `DROP COLUMN`）**显式报错、绝不静默跳过**；⑤ ⭐⭐ **幂等守卫** —— MySQL 8 **不支持** `ADD COLUMN IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS`，故列与索引**逐项查 `information_schema.COLUMNS` / `.STATISTICS`**（`TABLE_SCHEMA = DATABASE()`，不串库）后再加，两表用 `CREATE TABLE IF NOT EXISTS` → **重跑不报错、不重复加**；⑥ ⭐ **补齐内容**：`ab_supplier_dish_center_daily`（§3.6.1）+ `ab_message_template`（§3.9）**两表** · **9 列**（`ab_building.population` · `ab_supplier` 审核族 7 列 `audit_status`/`audit_remark`/`audited_at`/`audited_by`/`license_expire_at`/`invoice_title`/`takeout_links` · `ab_refund.order_status_before`）· **1 索引** `ab_building.idx_building_status` · `ab_building.status` **注释对齐三态**（`MODIFY` 只改注释**不改类型不迁数据**）；⑦ ⭐⭐ **一处文档错误被代码反向纠正** —— ER §5.3 把 `ab_refund.order_status_before` 记作 `tinyint`，而 `refund.service.ts` 写入的是 `order.status`（状态**字符串**）、`refund-admin.service.ts` 还会对它跑 `adminStatusText()` → 按 **`VARCHAR(16)`** 建列，**在 M5-2 同步更正 ER 文档**（文档记录错误，不是代码错）；⑧ ⭐⭐ **门禁自证能力（本次最有价值的一处设计）** —— 检查器**每次运行人为制造一个缺口**（从源A 深拷贝里删掉一列）**并确认能报出该列**，否则**自身失败**：「**恒绿的检查比没有检查更糟**」。**这条自检当场抓出了自身一个 bug**：计数器原在循环外模块级累加，第二支迁移文件拿本文件条数去比累计值 → 误报「CREATE TABLE 源码 2 / 解析 27」；⑨ **纳入门禁**：`gate.mjs` 的 `GATES` 增 `schema:parity`、`all` 别名数组同步 → **15/15 变 16/16**；反证测试（人为删 `population` 条目）→ 门禁红且**点名该列**（393 vs 394），还原后按 sha 校验一致；⑩ **文档回写**：ER 文档 §5.3 类型更正 + 新增 **§5.8 迁移↔实体对齐段**（缺口清单 / 为何长期未发现 / 增量补法 / 门禁 / 边界如实标注 **「ER 仍是唯一无自动校验的一份」**）+ 表数总览补警示「**表数对齐 ≠ 结构对齐**」；init 头注释「24 张表」**纠正为实际 25 张**并注明差额去向；运维手册 **R2 由「未验证」升级为「结构已机械对账 + 仍需真机空库首迁预演」**；⑪ ⚠️ **未做的事（如实标注）**：**仍未在真机空库上跑过首迁** —— 本机无 Docker / 无 MySQL，本批验的是「**迁移推演 ≡ 实体**」这件**结构事实**，**不是**「MySQL 上执行成功」这件**运行事实**；`down()` 只能回退本支；**两支迁移合并推演才等于实体**（单看 init 少 2 表 9 列，是刻意的）；⑫ `gate.mjs all verify` **16/16 全绿**（含 e2e m1/m2/m3）· 新增缺陷登记 **#76**（《缺陷与陷阱》，关单）· `SCAFFOLD_KEY` 增 **3 项** M5-2 契约载体。 |


---

*文档结束 · ABox 一盒 · 开发里程碑计划 v1.0 · 2026-09-14*

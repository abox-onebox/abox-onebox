/**
 * modules/finance/supplier-share.service.ts —— 供应商 / 集散中心 应付结算 占位骨架
 *
 * C10 + C11：只生成**应付结算单**并登记付款（不调支付通道）；
 * 结算周期 = **日结**（T+1 02:00 生成，财务按日付款并登记回单）。
 * 对公账户信息可后置收集，付款登记需支持「收款方式」备注。
 */
export {};
